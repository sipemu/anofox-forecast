---
phase: 04-prioritized-improvement-backlog-top-value-fixes
plan: 03
type: execute
wave: 3
depends_on: ["04-01", "04-02"]
files_modified:
  - .planning/baselines/BACKLOG.md
autonomous: true
requirements: [IMPR-01]
estimate:
  tokens: 40000
  raw_tokens: 20000
  tasks: 2
  confidence: high
must_haves:
  truths:
    - "A consolidated backlog ranks findings across all 8 measurement dimensions by value/effort/risk using REAL captured baseline numbers (IMPR-01)"
    - "Every ranked row records value, effort, risk, and its evidence/baseline source; the ranking rationale is documented"
    - "iai.json and criterion.json items are flagged manual-capture-pending with the exact capture command — no fabricated perf deltas"
    - "The backlog reflects what 04-01 and 04-02 LANDED vs DEFERRED (guards+coverage landed; accuracy gap #1 if 04-02 deferred, or noted landed)"
    - "V-04 (VAR error-variant divergence) is recorded as a known inconsistency, not refactored"
  artifacts:
    - .planning/baselines/BACKLOG.md
  key_links:
    - "Backlog inputs: coverage.json (real, post-ratchet), wasm_size.json (real), dhat.json (real), statsforecast_reference.json (real), accuracy.json (locked or absent), iai.json/criterion.json (placeholders), 03-GAP-INVENTORY.md (20 rows)"
---

<objective>
Author the consolidated, ranked improvement backlog (IMPR-01) — the closing artifact of the
Performance & Validation Hardening milestone. It ranks every finding across all 8 measurement
dimensions by value/effort/risk using the REAL captured baseline numbers, records the evidence
source per row, flags placeholder baselines as manual-capture-pending, and reflects exactly what
04-01 and 04-02 landed versus deferred.

Purpose: Turns the milestone's measurements into a prioritized, evidence-backed roadmap for the next
improvement cycle — every row traceable to a committed baseline number, no fabricated deltas.
Output: `.planning/baselines/BACKLOG.md`, ranked and rationale-documented.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@.planning/phases/04-prioritized-improvement-backlog-top-value-fixes/04-CONTEXT.md
@.planning/phases/04-prioritized-improvement-backlog-top-value-fixes/04-RESEARCH.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md

@.planning/baselines/coverage.json
@.planning/baselines/wasm_size.json
@.planning/baselines/dhat.json
@.planning/baselines/statsforecast_reference.json
@.planning/baselines/iai.json
@.planning/baselines/criterion.json
</context>

<artifacts_this_phase_produces>
- `.planning/baselines/BACKLOG.md` — the consolidated ranked improvement backlog (IMPR-01)
</artifacts_this_phase_produces>

<tasks>

<task type="auto">
  <name>Task 1: Read the landed/deferred outcomes and all real baseline numbers</name>
  <files>(no files modified — inputs gathering)</files>
  <read_first>
    - .planning/phases/04-prioritized-improvement-backlog-top-value-fixes/04-01-SUMMARY.md (what guards + coverage delta landed; the post-ratchet coverage.json floor)
    - .planning/phases/04-prioritized-improvement-backlog-top-value-fixes/04-02-SUMMARY.md (whether the accuracy gap was CLOSED+locked or STOPPED+deferred; the measured MASE)
    - .planning/baselines/coverage.json (post-04-01 real line-% and ratchet floor)
    - .planning/baselines/wasm_size.json (real: 2,838,958 bytes)
    - .planning/baselines/dhat.json (real peak_bytes per model family under benchmarks)
    - .planning/baselines/statsforecast_reference.json (real: monthly 0.8633, quarterly 1.1436, yearly 2.6954)
    - .planning/baselines/iai.json and criterion.json (PLACEHOLDERS — all zeros; do NOT read numeric deltas from them)
    - 03-GAP-INVENTORY.md (20 rows: V-01..V-04, G-01..G-10, A-01..A-05, plus the summary counts)
  </read_first>
  <action>
    Assemble the backlog inputs from the committed baselines and the two prior SUMMARYs. Determine the
    LANDED vs DEFERRED state precisely: 04-01 landed the V-01/V-02/V-03 guards + G-01 smoke test +
    coverage ratchet (mark these DONE/LANDED, not backlog candidates). 04-02 either CLOSED the accuracy
    gap (accuracy.json locked → mark the gap LANDED with its delta) or DEFERRED it (accuracy.json absent
    → the gap is the #1 backlog item). Read the 04-02 SUMMARY to decide which.

    Record the eight dimensions and their baseline status: (1) coverage.json real, (2) wasm_size.json
    real, (3) dhat.json real, (4) statsforecast_reference.json real, (5) accuracy.json locked-or-absent,
    (6) iai.json placeholder, (7) criterion.json placeholder, (8) 03-GAP-INVENTORY numerical-robustness
    rows real. Note the exact capture commands for the two placeholder baselines (from RESEARCH: the
    valgrind machine for iai, the quiet local machine for criterion) so their backlog rows are marked
    manual-capture-pending — never fabricate a numeric perf delta from placeholder zeros.
  </action>
  <verify>
    <automated>test -f .planning/baselines/coverage.json && test -f .planning/phases/04-prioritized-improvement-backlog-top-value-fixes/04-02-SUMMARY.md && echo "INPUTS_PRESENT" || echo "MISSING_INPUTS"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: the check prints INPUTS_PRESENT (coverage.json and the 04-02 SUMMARY both exist).
    - Source assertion: the LANDED/DEFERRED state of the accuracy gap is determined from the 04-02 SUMMARY (locked vs absent), not assumed.
  </acceptance_criteria>
  <done>All eight dimensions' real numbers and the 04-01/04-02 landed-vs-deferred outcomes are gathered and ready to rank.</done>
</task>

<task type="auto">
  <name>Task 2: Author and commit the ranked BACKLOG.md</name>
  <files>.planning/baselines/BACKLOG.md</files>
  <read_first>
    - 04-RESEARCH.md "Backlog Construction" section (the 8-dimension table, the ranking rubric: Value/Effort/Risk each 1-5, priority = Value × (Effort × Risk) / 5, real-evidence rows rank ahead of placeholders, and the recommended BACKLOG.md header/format)
  </read_first>
  <action>
    Write .planning/baselines/BACKLOG.md. Header: last-updated date and the Phase-4 inputs line naming
    each baseline file and its real/absent/placeholder status. Then a "Ranked Items" table with columns:
    Rank, ID, Title, Dimension, Value, Effort, Risk, Evidence (the specific baseline number/source),
    Status.

    Populate rows from every finding:
    - The accuracy MASE gap: ranked #1 IF the 04-02 SUMMARY shows deferred (evidence: anofox monthly
      MASE 1.0452 vs reference 0.8633; status Deferred with the root-cause note that AutoETS needs the
      seasonal period passed for monthly data); OR mark it Landed with its before/after delta IF 04-02
      locked accuracy.json.
    - The 04-01 landed items (V-01/V-02/V-03 guards, G-01 coverage): listed as LANDED this phase with
      the coverage before/after delta as evidence — not open backlog.
    - V-04 VAR error-variant divergence: a row documenting the InvalidParameter (raw-vec) vs
      MissingValues (TimeSeries) inconsistency as a KNOWN issue, effort low, status Open — explicitly
      NOT refactored this phase.
    - The P2 coverage gaps G-02, G-04..G-10 (evidence: per-file coverage % from 03-GAP-INVENTORY).
    - The P3 assertion-free tests A-01..A-05 and the ignored G-03 utility (weak-signal, low rank).
    - wasm-size (evidence 2,838,958 bytes) and dhat peak-memory (real per-family peak_bytes) as
      measured dimensions with any reduction opportunities.
    - iai.json and criterion.json rows: Status manual-capture-pending, Evidence = "placeholder zeros —
      requires <capture command>"; do NOT assign a numeric perf delta.

    Apply the rubric to score Value/Effort/Risk per row and order by the priority formula; document the
    ranking rationale (a short prose paragraph or per-row note explaining the value/effort/risk call and
    the evidence source). Real-evidence rows rank ahead of placeholder rows.

    Commit with git commit --no-verify (the cargo-fmt hook blocks doc commits): "docs(04): consolidated
    ranked improvement backlog across 8 measurement dimensions (IMPR-01)".
  </action>
  <verify>
    <automated>test -f .planning/baselines/BACKLOG.md && grep -qi "manual-capture-pending" .planning/baselines/BACKLOG.md && grep -qiE "V-04|VAR" .planning/baselines/BACKLOG.md && grep -qiE "1.0452|0.8633|MASE" .planning/baselines/BACKLOG.md && echo "BACKLOG_OK" || echo "BACKLOG_INCOMPLETE"</automated>
  </verify>
  <acceptance_criteria>
    - CLI output: the grep check prints BACKLOG_OK (file exists; contains manual-capture-pending; contains the V-04/VAR row; contains the accuracy MASE numbers).
    - Source assertion: BACKLOG.md has a Ranked Items table with Value/Effort/Risk/Evidence/Status columns and a documented ranking rationale.
    - Source assertion: iai/criterion rows carry no fabricated numeric delta — only the placeholder note and capture command.
    - Source assertion: `git log --oneline -1 -- .planning/baselines/BACKLOG.md` shows the backlog commit.
  </acceptance_criteria>
  <done>BACKLOG.md ranks all 8 dimensions with real evidence, flags placeholders manual-capture-pending, records V-04 as a known issue, reflects 04-01/04-02 landed-vs-deferred, and is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| baseline numbers → backlog rankings | A fabricated or misread number would produce an untrustworthy priority ordering |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-04-05 | Spoofing | placeholder iai/criterion rows | medium | mitigate | iai/criterion rows marked manual-capture-pending with the capture command; no numeric delta inferred from placeholder zeros |
| T-04-06 | Repudiation | backlog evidence provenance | low | mitigate | Each row cites its specific baseline file + number so any ranking is auditable against committed data |

No new package installs in this plan — package-legitimacy gate not applicable.
</threat_model>

<verification>
- BACKLOG.md exists and ranks findings across all 8 dimensions with Value/Effort/Risk/Evidence/Status.
- iai/criterion rows are manual-capture-pending with capture commands; no fabricated perf deltas.
- V-04 recorded as a known inconsistency; 04-01 items marked LANDED; accuracy gap ranked per 04-02 outcome.
- Committed as a deliberate doc commit.
</verification>

<success_criteria>
- IMPR-01 satisfied: consolidated ranked backlog using real captured numbers, rationale documented.
- Placeholder baselines flagged manual-capture-pending; no fabricated deltas.
- Landed-vs-deferred state from 04-01/04-02 faithfully reflected; V-04 documented not refactored.
</success_criteria>

<output>
Create `.planning/phases/04-prioritized-improvement-backlog-top-value-fixes/04-03-SUMMARY.md` when done,
recording: the backlog location, the top-ranked items with their evidence sources, the placeholder
flags, and how 04-01/04-02 outcomes are reflected in the ranking.
</output>
