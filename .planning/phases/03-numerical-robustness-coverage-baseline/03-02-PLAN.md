---
phase: 03-numerical-robustness-coverage-baseline
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - tests/property_robustness.rs
  - .proptest-regressions/
autonomous: true
requirements: [ROBUST-03]
user_setup: []

estimate:
  tokens: 58000
  raw_tokens: 29000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "changepoint::metrics {precision_recall, hausdorff, randindex} never panic and never return NaN across bounded random valid breakpoint sets — proven by proptest in tests/property_robustness.rs."
    - "MSTL::decompose(&[f64]) never panics for any bounded random f64 slice (including constant, all-zeros, extreme-scale); when it returns Some, every trend/seasonal/remainder value is finite and lengths equal input length."
    - "CvFoldGenerator::generate(series_len) never panics for any bounded parameter combination; when it returns Ok(folds), every fold satisfies train_end <= test_start and test_end <= series_len (no temporal leakage)."
    - "All proptest blocks are bounded (with_cases <= 100; MSTL/CV heavier blocks <= 30/100) so the suite runs under ~30s; the .proptest-regressions/ corpus directory is committed, not gitignored (ROBUST-03)."
  artifacts:
    - "tests/property_robustness.rs"
    - ".proptest-regressions/ (committed if a counterexample is ever recorded; directory tracked)"
  key_links:
    - "tests/property_robustness.rs → src/changepoint/metrics.rs::{precision_recall,hausdorff,randindex}"
    - "tests/property_robustness.rs → src/seasonality/mstl.rs::MSTL::decompose (returns Option)"
    - "tests/property_robustness.rs → src/utils/cross_validation.rs::CvFoldGenerator::generate"
---

<objective>
Add the property-based robustness suite (ROBUST-03) for the three ROADMAP-named fragile areas —
changepoint metrics, MSTL decomposition, and CV boundary conditions — asserting no-panic and
no-NaN invariants across bounded random inputs. Reuses the established proptest patterns from
tests/property_tests.rs (with_cases bound, make_ts idiom) and the exact strategies documented in
03-RESEARCH.md §Proptest Strategies.

Purpose: Prove the "known-fragile" numerical paths hold their invariants under randomized inputs,
not just hand-picked cases — the property-testing complement to Plan 03-01's deterministic
edge-case matrix. This closes ROBUST-03 (SC-3).
Output: tests/property_robustness.rs (single new integration suite) plus a committed
.proptest-regressions/ corpus directory.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-CONTEXT.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md
@tests/property_tests.rs
@src/changepoint/metrics.rs
@src/seasonality/mstl.rs
@src/utils/cross_validation.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — one proptest block, changepoint self-match, end-to-end green</name>
  <files>tests/property_robustness.rs</files>
  <read_first>
    - tests/property_tests.rs (lines 1-22: imports + proptest prelude; lines 68-80: proptest! block + ProptestConfig::with_cases(50) idiom)
    - src/changepoint/metrics.rs (lines 33, 94, 127: precision_recall / hausdorff / randindex signatures — all return Result<..>)
    - src/changepoint/mod.rs (line 64: pub use metrics::{hausdorff, precision_recall, randindex, PrecisionRecall} — the import path)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (§Proptest Strategies — Changepoint block; §Runtime Bounding; §Proptest Seed Stability)
  </read_first>
  <action>
    Create tests/property_robustness.rs. Import `proptest::prelude::*` and the changepoint metrics via
    `anofox_forecast::changepoint::{precision_recall, hausdorff, randindex}` (re-exported at that path per
    changepoint/mod.rs line 64). Wire exactly ONE proptest block guarded by
    `ProptestConfig::with_cases(50)` containing a single property test
    `fn changepoint_precision_recall_self_match_is_perfect(n in 50usize..200, n_bkps in 1usize..5)`:
    generate strictly-increasing valid breakpoints with a terminal breakpoint equal to n (step = n/(n_bkps+1);
    map 1..=n_bkps to i*step; push n), call `precision_recall(&bkps, &bkps, 0)`, bind the Ok with
    prop_assert (NOT `.unwrap()` at top level — use `?` or a prop_assert on is_ok first, then read fields),
    and prop_assert precision≈1.0, recall≈1.0 (abs diff < 1e-10) and f1.is_finite(). This single block proves
    the file compiles, the import path resolves, and the prop_assert idiom works against the real Result-returning
    metric before expansion. Do NOT add MSTL or CV blocks yet.
  </action>
  <verify>
    <automated>cargo test --test property_robustness --all-features changepoint_precision_recall_self_match_is_perfect</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test property_robustness --all-features changepoint_precision_recall_self_match_is_perfect` compiles and passes (1 passed).
    - The proptest block declares a bounded case count: `grep -qE 'with_cases\([0-9]+\)' tests/property_robustness.rs` succeeds.
    - The file imports the metrics from the re-export path: `grep -q 'changepoint::{' tests/property_robustness.rs` OR `grep -qE 'use anofox_forecast::changepoint::(precision_recall|hausdorff|randindex)' tests/property_robustness.rs` succeeds.
  </acceptance_criteria>
  <done>property_robustness.rs exists; the changepoint self-match property passes end-to-end and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Expand — changepoint reflexive + MSTL + CV property blocks (no-panic, no-NaN, temporal integrity)</name>
  <files>tests/property_robustness.rs</files>
  <read_first>
    - tests/property_robustness.rs (the tracer block from Task 1)
    - src/seasonality/mstl.rs (line 100: MSTL::new(seasonal_periods: Vec<usize>); line 130: decompose(&self, series: &[f64]) -> Option<MSTLResult>; line 13: MSTLResult fields trend/seasonal/remainder)
    - src/seasonality/mod.rs (line 43: pub use mstl::{MSTLResult, MSTL} — the import path)
    - src/utils/cross_validation.rs (line 139: CvFoldGenerator::new; builders n_folds/horizon/min_initial_window/gap; line 233: generate(&self, series_len) -> Result<Vec<Fold>>; Fold fields train_end/test_start/test_end at lines 36/38/40; train_size at line 45)
    - src/utils/mod.rs (line 18-21: pub use cross_validation::{CvFoldGenerator, Fold, ...} — the import path)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (§Proptest Strategies — Changepoint/MSTL/CV blocks; Pitfall 6: MSTL returns Option not Result; §Runtime Bounding)
  </read_first>
  <action>
    Expand tests/property_robustness.rs with the remaining property blocks.

    Changepoint (same with_cases(50) block or a sibling block): add
    `changepoint_hausdorff_reflexive(n in 10usize..200, n_bkps in 1usize..4)` asserting
    `hausdorff(&bkps, &bkps)` ≈ 0.0 (abs < 1e-12), and
    `changepoint_randindex_self_is_one(n in 10usize..200, n_bkps in 1usize..4)` asserting
    `randindex(&bkps, &bkps, n)` ≈ 1.0 (abs < 1e-10) and is_finite. Use the same valid-breakpoint
    generator as Task 1 (strictly increasing, terminal == n). Do NOT feed malformed breakpoints to
    proptest (empty bkps / terminal != n are error paths tested deterministically elsewhere, per RESEARCH
    §Changepoint note on last().unwrap() guarding).

    MSTL (import `anofox_forecast::seasonality::MSTL`; block guarded by `ProptestConfig::with_cases(30)`
    because fitting is heavier): add `mstl_decompose_never_panics(values in prop::collection::vec(-1000.0f64..1000.0, 10..100), period in 2usize..8)`
    — construct `MSTL::new(vec![period])`, call `.decompose(&values)`, and handle the `Option` with
    `if let Some(r) = result { ... }` (MSTL returns Option, NOT Result — a None for short series is a
    valid outcome, never asserted as failure). Inside Some: prop_assert every value in trend, each
    seasonal[i], and remainder is `is_finite()`, and prop_assert_eq r.trend.len() == values.len(). Add a
    second block `mstl_decompose_constant_series(n in 10usize..100, c in -1000.0f64..1000.0, period in 2usize..8)`
    over `vec![c; n]` asserting finite trend/remainder when Some.

    CV (import `anofox_forecast::utils::CvFoldGenerator`; block guarded by `ProptestConfig::with_cases(100)`):
    add `cv_generate_never_panics(series_len in 0usize..500, horizon in 1usize..50, n_folds in 1usize..10, min_window in 2usize..50, gap in 0usize..10)`
    — build `CvFoldGenerator::new().n_folds(n_folds).horizon(horizon).min_initial_window(min_window).gap(gap).generate(series_len)`,
    and `if let Ok(folds) = result` prop_assert for every fold: `fold.train_end <= fold.test_start` (no
    temporal leakage), `fold.test_end <= series_len`, and `fold.train_size() >= min_window`. Err is an
    acceptable outcome (never a panic).

    Every fallible call in these blocks is handled by `if let Ok/Some`, prop_assert, or the valid-input
    generator — no top-level `.unwrap()` on a call that may legitimately fail. Keep each block's case count
    at or below the RESEARCH-documented bound so total file runtime stays under ~30s.
  </action>
  <verify>
    <automated>cargo test --test property_robustness --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test property_robustness --all-features` passes with zero panics and zero failures.
    - All three fragile areas are covered: `grep -q changepoint tests/property_robustness.rs && grep -qi mstl tests/property_robustness.rs && grep -q cv_generate tests/property_robustness.rs` all succeed.
    - MSTL is handled as Option (no Result matching on decompose): `grep -qE 'if let Some' tests/property_robustness.rs` succeeds AND `grep -nE '\.decompose\([^)]*\)\??\.unwrap' tests/property_robustness.rs` returns no matches.
    - The CV temporal-integrity invariant is asserted: `grep -q 'train_end' tests/property_robustness.rs && grep -q 'test_start' tests/property_robustness.rs` succeeds.
    - No top-level unwrap on decompose/generate: `grep -nE '\.(decompose|generate)\([^)]*\)\??\.(unwrap|expect)' tests/property_robustness.rs` returns no matches.
  </acceptance_criteria>
  <done>All three fragile areas have bounded proptest blocks asserting no-panic + no-NaN (+ CV temporal integrity); the suite is green.</done>
</task>

<task type="auto">
  <name>Task 3: Commit the proptest regression corpus so CI seeds are stable</name>
  <files>.proptest-regressions/</files>
  <read_first>
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (Pitfall 4: Proptest Seed Flakiness; §Proptest Seed Stability — commit .proptest-regressions/, do NOT gitignore)
    - .gitignore (confirm .proptest-regressions/ is NOT ignored; add a tracked directory if the corpus is empty)
  </read_first>
  <action>
    Ensure the proptest regression corpus is version-controlled so a shrunk counterexample discovered in
    CI does not silently change between runs (RESEARCH Pitfall 4). Check `.gitignore` for any
    `.proptest-regressions` entry and REMOVE it if present (this project must track the corpus). If proptest
    produced a `.proptest-regressions/` directory during Task 2 (only happens if a case failed and shrank),
    `git add` it. If no counterexample was recorded, create the directory tracked by git with a
    `.proptest-regressions/.gitkeep` placeholder so the convention is established from the start (per RESEARCH:
    "add it now when first introducing the file, not after a failure"). Do NOT weaken any assertion to make a
    case pass — a recorded counterexample means a real invariant break to file as a gap, not to silence.
  </action>
  <verify>
    <automated>test ! -f .gitignore || ! grep -q 'proptest-regressions' .gitignore; git check-ignore .proptest-regressions 2>/dev/null; test $? -ne 0</automated>
  </verify>
  <acceptance_criteria>
    - `.proptest-regressions/` is NOT git-ignored: `git check-ignore .proptest-regressions` exits non-zero (path is tracked/trackable).
    - No `proptest-regressions` line remains in `.gitignore`: `test ! -f .gitignore || ! grep -q proptest-regressions .gitignore` succeeds.
    - The directory is present and tracked: `git ls-files .proptest-regressions/ | head -1` prints at least one path (a recorded corpus file or `.gitkeep`).
  </acceptance_criteria>
  <done>The proptest regression corpus directory is tracked in git and un-ignored; CI seeds are stable across runs.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| random generator → numerical fn | proptest-generated `&[usize]` / `&[f64]` / CV params cross into changepoint metrics, MSTL, and fold arithmetic |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-04 | Denial of Service | Panic on random malformed input in changepoint/MSTL/CV numeric paths | high | mitigate | Bounded proptest blocks assert no-panic across randomized inputs (Tasks 1-2); any panic → P1 gap-inventory row in Plan 03-03 |
| T-03-05 | Tampering | Silent NaN/Inf in metric output or decomposition components | medium | mitigate | prop_assert is_finite on every metric field and every MSTL component (Task 2) |
| T-03-06 | Tampering | Temporal leakage (train window overlaps test) in CV fold arithmetic | medium | mitigate | prop_assert train_end <= test_start and test_end <= series_len for every fold (Task 2) |
| T-03-07 | Repudiation | Flaky CI from unstable proptest seeds hides a real counterexample | low | mitigate | Commit .proptest-regressions/ corpus so discovered cases replay deterministically (Task 3) |
</threat_model>

<verification>
- `cargo test --test property_robustness --all-features` — green, zero panics, under ~30s.
- `cargo clippy --all-features -- -D warnings` — clean (new test file introduces no warnings).
- `.proptest-regressions/` is tracked and not git-ignored.
- No top-level `.unwrap()`/`.expect()` on any `.decompose(`/`.generate(` call.
</verification>

<success_criteria>
- ROBUST-03: proptest property tests cover changepoint metrics, MSTL decomposition, and CV boundary
  conditions, asserting no-panic and no-NaN invariants (plus CV temporal integrity) across bounded random
  inputs; the regression corpus is committed for seed stability (SC-3).
</success_criteria>

<output>
Create `.planning/phases/03-numerical-robustness-coverage-baseline/03-02-SUMMARY.md` when done.
Record in the SUMMARY: any proptest counterexample that was recorded (file + shrunk input + the invariant
it broke) so Plan 03-03 can file it as a P1 gap-inventory row; and the observed per-block case counts /
total suite runtime.
</output>
</content>
</invoke>
