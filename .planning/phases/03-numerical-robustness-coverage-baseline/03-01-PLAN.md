---
phase: 03-numerical-robustness-coverage-baseline
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/edge_case_robustness.rs
  - src/models/laplace/gpd_tails.rs
  - src/models/laplace/multiscale.rs
autonomous: true
requirements: [ROBUST-01, ROBUST-02]
estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Every representative model returns an Err (never panics) on empty, NaN/Inf, and n=2 inputs — proven by tests/edge_case_robustness.rs passing under cargo test."
    - "NaN/Inf inputs deterministically return ForecastError::MissingValues from every model that calls validate_series_complete()."
    - "gpd_tails.rs and multiscale.rs call validate_series_complete(series) at the top of fit() before any delegation (ROBUST-02 inline fixes)."
    - "The 4 raw-vec global models (global_ets, global_croston, global_theta, var.rs) are NOT force-refactored here; they are recorded for the Plan-03 gap inventory as P1 items."
  artifacts:
    - "tests/edge_case_robustness.rs"
    - "src/models/laplace/gpd_tails.rs (validate call added)"
    - "src/models/laplace/multiscale.rs (validate call added)"
  key_links:
    - "tests/edge_case_robustness.rs → src/models/{family}/*.rs fit() → src/models/traits.rs::validate_series_complete"
    - "NaN/Inf edge case → TimeSeries::has_missing_values() → ForecastError::MissingValues"
---

<objective>
Stand up the edge-case robustness integration suite (ROBUST-01) and land the two trivial inline
validation fixes (ROBUST-02). One representative model per family is driven through the full
ROADMAP edge-case input set (constant, n=2, all-zeros/intermittent, NaN/Inf, zero-length,
extreme-scale); each test asserts the exact `ForecastError` variant where deterministic, else
`is_err()` + no-panic. No test may trigger a panic.

Purpose: Prove "handles bad input safely" is measured, not asserted — the phase's core-value
contribution. Fix the 2 delegation wrappers whose fit() reaches an inner forecaster without an
explicit pre-estimation guard; file the 4 raw-vec global models as P1 backlog (handled in Plan 03).
Output: `tests/edge_case_robustness.rs`, inline `validate_series_complete(series)?;` in
`gpd_tails.rs` and `multiscale.rs`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-CONTEXT.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md
@src/error.rs
@src/models/traits.rs
@tests/property_tests.rs
@tests/laplace_robustness.rs
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — one model, one edge case, end-to-end (Naive + NaN → MissingValues)</name>
  <files>tests/edge_case_robustness.rs</files>
  <read_first>
    - tests/property_tests.rs (lines 1-22 for the make_ts helper + imports to replicate)
    - src/error.rs (ForecastError variants — assertion targets)
    - src/models/traits.rs (validate_series_complete: only checks has_missing_values → MissingValues)
  </read_first>
  <action>
    Create tests/edge_case_robustness.rs. Add module-level imports mirroring property_tests.rs:
    `anofox_forecast::core::TimeSeries`, `anofox_forecast::models::Forecaster`, `anofox_forecast::error::ForecastError`,
    `anofox_forecast::models::baseline::Naive`, and chrono `Duration, TimeZone, Utc`.
    Replicate the `make_ts(values: &[f64]) -> TimeSeries` helper verbatim from property_tests.rs
    (constructs hourly timestamps; the `.unwrap()` is on the CONSTRUCTOR only — never on fit/predict).
    Wire exactly ONE end-to-end path: a `#[test] fn naive_nan_returns_missing_values()` that builds a
    30-point series with one `f64::NAN` element, calls `Naive::new().fit(&ts)`, and asserts
    `matches!(result, Err(ForecastError::MissingValues))` with a descriptive message. Do NOT use `.unwrap()`
    on `fit()`. No other models or cases yet — this task proves the harness compiles and the assertion idiom
    (structural `matches!`, not string matching) works against the real error type before expansion.
  </action>
  <verify>
    <automated>cargo test --test edge_case_robustness --all-features naive_nan_returns_missing_values -- --exact</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test edge_case_robustness --all-features naive_nan_returns_missing_values -- --exact` compiles and passes (1 passed).
    - The test body contains no `.unwrap()` on `.fit(` and no `.expect(` on `.fit(`: `grep -nE '\.fit\([^)]*\)\??\.(unwrap|expect)' tests/edge_case_robustness.rs` returns no matches.
  </acceptance_criteria>
  <done>Harness file exists; the Naive+NaN→MissingValues path passes end-to-end and is committed.</done>
</task>

<task type="auto">
  <name>Task 2: Expand — full edge-case matrix across representative models per family</name>
  <files>tests/edge_case_robustness.rs</files>
  <read_first>
    - tests/edge_case_robustness.rs (the tracer harness from Task 1)
    - src/error.rs (EmptyData, InsufficientData, MissingValues, FitRequired variants)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (Representative Model Selection table; Assertion Patterns; Pitfall 1 — validate_series_complete does NOT check empty/length)
  </read_first>
  <action>
    Expand the harness to one representative model per family, each driven through the ROADMAP edge-case
    inputs. Representatives (per RESEARCH selection table): `AutoETS` (exponential), `ARIMA::new(1,0,1)` (arima),
    `Theta::new()` (theta), `TBATS::new(12)` (tbats), `Naive::new()` (baseline), `Croston::new()` (intermittent),
    `MSTLForecaster::new(vec![12])` (MSTL, flagged fragile), `GARCH::new()` (distributional/extreme-scale risk),
    and — behind `#[cfg(feature = "distributional")]` — `LaplaceForecaster` and a VAR representative for the
    EmptyData/InsufficientData paths. Define shared value fixtures at module scope: constant `vec![5.0;30]`,
    `n2 = vec![1.0,2.0]`, `zeros = vec![0.0;30]`, an intermittent sparse vector, `with_nan` (one NaN),
    `with_inf` (one INFINITY), `extreme_large = vec![1e15;30]`, `extreme_small = vec![1e-15;30]`, and an empty
    series via `make_ts(&[])`.
    Assertion rules (per D-locked CONTEXT + RESEARCH):
    - NaN/Inf → assert exact `ForecastError::MissingValues` for every model that calls validate_series_complete.
    - Empty series → assert exact variant the model actually returns (EmptyData or InsufficientData); do NOT
      assume EmptyData universally (validate_series_complete does not check emptiness). Discover the real
      variant empirically and assert it exactly.
    - n=2 below a model minimum → assert `matches!(.., Err(ForecastError::InsufficientData { .. }))`.
    - constant / extreme-scale where success is legitimate → assert no-panic: if `fit()` returns Ok, call
      `predict(1)` and assert every value `is_finite()`; if Err, that is acceptable (no panic).
    Every fit()/predict() call is guarded by `assert!(result.is_err())`, `matches!`, or an `if let Ok(..)`
    finite-check — never `.unwrap()` on a fallible-on-purpose call. Name tests `{family}_{condition}`.
    Gate distributional-only models behind `#[cfg(feature = "distributional")]` so the no-feature build still compiles.
    Record (as `// GAP:` inline comments) any model that PANICS instead of returning Err — do NOT patch such a
    model here beyond the two Task-3 delegation fixes; these panics become P1 gap-inventory rows in Plan 03.
  </action>
  <verify>
    <automated>cargo test --test edge_case_robustness --all-features</automated>
  </verify>
  <acceptance_criteria>
    - `cargo test --test edge_case_robustness --all-features` passes with zero panics and zero failures.
    - The suite covers ≥ 8 model families: `grep -cE '^\s*fn (autoets|arima|theta|tbats|naive|croston|mstl|garch|laplace|var)_' tests/edge_case_robustness.rs` returns ≥ 8 (after stripping comment lines with `grep -v '^\s*//'`).
    - Every ROADMAP edge case appears as a test: `grep -qi nan tests/edge_case_robustness.rs && grep -qi inf tests/edge_case_robustness.rs && grep -qi empty tests/edge_case_robustness.rs && grep -qi constant tests/edge_case_robustness.rs && grep -qi extreme tests/edge_case_robustness.rs` all succeed.
    - No `.unwrap()`/`.expect()` on any `.fit(`/`.predict(` call: `grep -nE '\.(fit|predict)\([^)]*\)\??\.(unwrap|expect)' tests/edge_case_robustness.rs` returns no matches.
  </acceptance_criteria>
  <done>All representative families are exercised against the full edge-case set; suite is green with no panics; any observed panic is annotated `// GAP:` for Plan-03 inventory.</done>
</task>

<task type="auto">
  <name>Task 3: ROBUST-02 inline fixes — add validate_series_complete to the two delegation wrappers</name>
  <files>src/models/laplace/gpd_tails.rs, src/models/laplace/multiscale.rs</files>
  <read_first>
    - src/models/laplace/gpd_tails.rs (fit at line ~452 — delegates to self.inner.fit(series))
    - src/models/laplace/multiscale.rs (fit at line ~188 — computes n from series then decimates)
    - src/models/traits.rs (validate_series_complete signature and export path: crate::models::traits)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (Fix-vs-File Decision Rule — ONLY these two files are fixed inline; the 4 raw-vec globals are P1, filed in Plan 03)
  </read_first>
  <action>
    In both `gpd_tails.rs` and `multiscale.rs`, import `validate_series_complete` from `crate::models::traits`
    (both files already `use crate::models::traits::Forecaster;` — extend that use to include
    `validate_series_complete`). Insert `validate_series_complete(series)?;` as the FIRST statement inside the
    `fn fit(&mut self, series: &TimeSeries) -> Result<()>` body — before the inner delegation in gpd_tails and
    before the `series.primary_values().len()` read in multiscale. This makes the pre-estimation NaN/Inf guard
    explicit at the wrapper boundary (per D-locked fix-inline policy) rather than relying on indirect delegation.
    Do NOT touch global_ets.rs, global_croston.rs, global_theta.rs, or var.rs — those are raw-`&[Vec<f64>]`
    APIs requiring a different validation approach and are filed as P1 in the Plan-03 gap inventory. Keep clippy
    green (no unused imports).
  </action>
  <verify>
    <automated>cargo test --all-features -p anofox-forecast laplace && cargo clippy --all-features -- -D warnings</automated>
  </verify>
  <acceptance_criteria>
    - Both files call the guard at fit() entry: `grep -c 'validate_series_complete(series)' src/models/laplace/gpd_tails.rs` ≥ 1 AND `grep -c 'validate_series_complete(series)' src/models/laplace/multiscale.rs` ≥ 1.
    - The four raw-vec globals are untouched by this task: `git diff --name-only` does NOT list global_ets.rs, global_croston.rs, global_theta.rs, or var.rs.
    - `cargo clippy --all-features -- -D warnings` is clean (no unused-import warning from the added import).
    - Existing laplace tests still pass: `cargo test --all-features -p anofox-forecast laplace` is green.
  </acceptance_criteria>
  <done>gpd_tails and multiscale validate NaN/Inf at fit() entry explicitly; clippy is green; the 4 global raw-vec models are deferred to the Plan-03 P1 gap inventory.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| caller → `fit()` | Untrusted `TimeSeries` values (NaN/Inf/empty/extreme) cross into parameter estimation |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-01 | Tampering | NaN/Inf propagation into fitted params (gpd_tails, multiscale fit) | high | mitigate | Explicit `validate_series_complete(series)?;` at fit() entry (Task 3) |
| T-03-02 | Denial of Service | Panic on malformed input (library-level DoS) | high | mitigate | Edge-case suite asserts `is_err()` not panic for all malformed inputs (Tasks 1-2); observed panics → P1 gap rows |
| T-03-03 | Tampering | Raw-vec global models accept NaN floats unguarded | medium | transfer | Filed as P1 backlog (Plan 03 gap inventory) — non-trivial internal-API refactor, out of scope this phase per D-locked fix-vs-file policy |
</threat_model>

<verification>
- `cargo test --test edge_case_robustness --all-features` — green, zero panics.
- `cargo clippy --all-features -- -D warnings` — clean.
- `cargo test --all-features -p anofox-forecast laplace` — existing laplace tests still pass.
- No `.unwrap()`/`.expect()` on any fallible-on-purpose `.fit(`/`.predict(` call in the new suite.
</verification>

<success_criteria>
- ROBUST-01: one representative model per family exercised against constant, n=2, all-zeros/intermittent,
  NaN/Inf, zero-length, extreme-scale — exact `ForecastError` variant where deterministic, else `is_err()`;
  no panic.
- ROBUST-02 (inline portion): gpd_tails.rs and multiscale.rs guard fit() with validate_series_complete;
  the 4 raw-vec globals recorded for Plan-03 P1 gap inventory.
</success_criteria>

<output>
Create `.planning/phases/03-numerical-robustness-coverage-baseline/03-01-SUMMARY.md` when done.

**MANDATORY handoff contract for Plan 03-03 (the gap inventory consumes this SUMMARY).** The SUMMARY
MUST contain a clearly-headed `## Gap-Inventory Handoff` section recording all three of:
1. **Empty-series variant per model** — a row per representative model naming the exact
   `ForecastError` variant it returned on the empty input (for the Plan-03 inventory).
2. **Panicking models** — every model annotated `// GAP:` in the suite (i.e. that PANICKED instead of
   returning `Err`), listed by name, so Plan 03 can file it P1. If none panicked, state "none".
3. **Deferred P1 raw-vec models (explicit, by name)** — the four models NOT fixed inline and handed to
   the Plan-03 P1 gap inventory: `src/models/exponential/global_ets.rs`,
   `src/models/intermittent/global_croston.rs`, `src/models/theta/global_theta.rs`, `src/models/var.rs`
   — each with the risk (unguarded `&[Vec<f64>]` NaN/Inf into fitted params).

Acceptance for this contract (verify before the SUMMARY commit):
`grep -q 'Gap-Inventory Handoff' 03-01-SUMMARY.md` AND `grep -q global_ets 03-01-SUMMARY.md &&
grep -q global_croston 03-01-SUMMARY.md && grep -q global_theta 03-01-SUMMARY.md &&
grep -q 'var.rs' 03-01-SUMMARY.md` all succeed. This is a hard requirement — Plan 03-03 Task 2 fails
its input guard if this section is absent.
</output>
