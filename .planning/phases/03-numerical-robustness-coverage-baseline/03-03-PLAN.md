---
phase: 03-numerical-robustness-coverage-baseline
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - scripts/update_coverage.sh
  - .planning/baselines/coverage.json
  - .github/workflows/ci.yml
  - .planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md
autonomous: false
requirements: [COVER-01, COVER-02]
user_setup: []

estimate:
  tokens: 66000
  raw_tokens: 33000
  tasks: 4
  confidence: med

must_haves:
  truths:
    - "scripts/update_coverage.sh captures line coverage via `cargo llvm-cov --package anofox-forecast --all-features --json --summary-only` and writes .planning/baselines/coverage.json with a provenance block matching the criterion.json convention (git_sha, timestamp_iso, rustc_version, cargo_llvm_cov_version, host_cpu, host_os, active_features) plus lines_total/lines_covered/lines_percent/ratchet_floor_percent (COVER-01)."
    - ".planning/baselines/coverage.json is committed with a real measured floor (ratchet_floor_percent = lines_percent - ~1.0, up-only) and its invocation string documents the exact `--package anofox-forecast --all-features` scope for reproducibility."
    - "The EXISTING ci.yml `coverage:` job is EXTENDED (not replaced, no new workflow) with a step that reads ratchet_floor_percent from coverage.json and passes it to `cargo llvm-cov --fail-under-lines <floor>` so CI FAILS below the floor (COVER-01 / SC-4)."
    - "03-GAP-INVENTORY.md is committed with file + function + missing-invariant rows structured for Phase 4 backlog consumption, and INCLUDES the P1 rows 03-01 deferred (global_ets, global_croston, global_theta, var.rs raw-vec models) plus any model 03-01 or 03-02 annotated as panicking, plus an assertion-density / assertion-free-test section (COVER-02 / SC-5)."
  artifacts:
    - "scripts/update_coverage.sh"
    - ".planning/baselines/coverage.json"
    - ".github/workflows/ci.yml (coverage: job extended)"
    - ".planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md"
  key_links:
    - "scripts/update_coverage.sh → .planning/baselines/coverage.json (writes ratchet_floor_percent)"
    - ".github/workflows/ci.yml coverage: job → .planning/baselines/coverage.json (reads floor) → cargo llvm-cov --fail-under-lines"
    - "03-01-SUMMARY.md (panicking models + P1 raw-vec deferrals) → 03-GAP-INVENTORY.md P1 rows → Phase 4 backlog"
    - "03-02-SUMMARY.md (any proptest counterexample) → 03-GAP-INVENTORY.md P1 rows"
---

<objective>
Capture and commit the line-coverage baseline (COVER-01) and produce the coverage gap inventory
(COVER-02). Stand up `scripts/update_coverage.sh` (mirroring the existing `update_criterion.sh` /
`update_wasm_size.sh` provenance convention), write `.planning/baselines/coverage.json` with a real
measured floor, extend the EXISTING `ci.yml` coverage job with a ratchet hard-gate, and author
`03-GAP-INVENTORY.md` structured to seed the Phase 4 backlog — pulling forward the P1 items 03-01
deferred and any panicking models 03-01/03-02 recorded.

Purpose: Lock a reproducible, up-only coverage floor that CI enforces (SC-4) and hand Phase 4 a
machine-usable gap list (SC-5). This is the phase's measurement + hard-gate deliverable — committing
the baseline and adding a CI hard-gate is a one-way-door decision, so it is gated behind a decision
checkpoint (mirroring Phase 2's accuracy.json baseline-lock pattern).
Output: scripts/update_coverage.sh, .planning/baselines/coverage.json, extended ci.yml coverage: job,
03-GAP-INVENTORY.md.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-CONTEXT.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md
@.planning/phases/03-numerical-robustness-coverage-baseline/03-01-PLAN.md
@.planning/baselines/criterion.json
@.github/workflows/ci.yml
@scripts/update_criterion.sh
</context>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — capture coverage once end-to-end and write coverage.json with provenance</name>
  <files>scripts/update_coverage.sh, .planning/baselines/coverage.json</files>
  <read_first>
    - scripts/update_criterion.sh (the established provenance-capture script pattern to mirror: git_sha, timestamp, rustc, host_cpu/os)
    - .planning/baselines/criterion.json (the provenance block field names to match exactly)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (§Coverage Baseline & Ratchet — canonical invocation, coverage.json schema, update_coverage.sh reference; §Open Question 2 — recommend scoping to --package anofox-forecast; Assumptions A2/A5 — jq/bc fallbacks)
  </read_first>
  <action>
    Create `scripts/update_coverage.sh` (chmod +x, `set -euo pipefail`, `cd` to `git rev-parse --show-toplevel`)
    following the update_criterion.sh provenance idiom. It runs the canonical scoped invocation
    `cargo llvm-cov --package anofox-forecast --all-features --json --summary-only --output-path /tmp/coverage_summary.json`
    (scope to the main crate per RESEARCH Open Question 2 so the bench-harness/js crates do not distort the line %;
    document this scope in the invocation string). Extract `lines.percent`, `lines.covered`, `lines.count` from
    `.data[0].totals.lines` with `jq` (fall back to a `python3 -c` json read if jq is absent, per Assumption A2).
    Compute `ratchet_floor_percent = lines_percent - 1.0` (use `python3` for the float subtract if `bc` is
    unavailable, per Assumption A5), rounded to one decimal. Write `.planning/baselines/coverage.json` with a
    `provenance` block whose keys match criterion.json (git_sha, timestamp_iso, rustc_version,
    cargo_llvm_cov_version, host_cpu, host_os, active_features) plus a `coverage` block
    {metric:"line", invocation, lines_total, lines_covered, lines_percent, ratchet_floor_percent}.
    Run the script ONCE to produce the real baseline file. This tracer proves the full capture→write path
    end-to-end before the CI wiring and gap-inventory expansion depend on it.
    Do NOT edit ci.yml in this task. Do NOT commit coverage.json yet — the commit is gated behind the Task 3
    decision checkpoint.
  </action>
  <verify>
    <automated>bash scripts/update_coverage.sh && jq -e '.coverage.ratchet_floor_percent and .coverage.lines_percent and .provenance.git_sha' .planning/baselines/coverage.json</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/update_coverage.sh` exits 0 and writes `.planning/baselines/coverage.json`.
    - The baseline has a measured (non-placeholder) coverage: `jq -e '.coverage.lines_percent > 0' .planning/baselines/coverage.json` succeeds.
    - The floor is below the measured coverage (up-only ratchet margin): `jq -e '.coverage.ratchet_floor_percent < .coverage.lines_percent' .planning/baselines/coverage.json` succeeds.
    - The invocation string documents the scoped feature set: `jq -r '.coverage.invocation' .planning/baselines/coverage.json | grep -q -- '--package anofox-forecast'` AND `... | grep -q -- '--all-features'`.
    - The provenance block mirrors criterion.json keys: `jq -e '.provenance.git_sha and .provenance.rustc_version and .provenance.cargo_llvm_cov_version and .provenance.host_os' .planning/baselines/coverage.json` succeeds.
    - The script is executable: `test -x scripts/update_coverage.sh`.
  </acceptance_criteria>
  <done>update_coverage.sh captures line coverage end-to-end and writes a real coverage.json baseline with a measured floor; committed via the Task 3 checkpoint, not here.</done>
</task>

<task type="auto">
  <name>Task 2: Build the coverage gap inventory (03-GAP-INVENTORY.md) from coverage + 03-01/03-02 findings</name>
  <files>.planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md</files>
  <read_first>
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-01-SUMMARY.md (the exact empty-series ForecastError variant per model; any model annotated `// GAP:` as PANICKING; the 4 raw-vec P1 deferrals)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-02-SUMMARY.md (any recorded proptest counterexample + the invariant it broke)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (§Gap Inventory Format — schema, P1/P2/P3 priority rubric; §Assertion Density Detection; Pitfall 5 — WASM-only lines are NOT gaps; validate_series_complete audit table — the 6 non-calling files)
    - .planning/baselines/coverage.json (the coverage % at inventory time)
  </read_first>
  <action>
    Author `.planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md` using the
    RESEARCH §Gap Inventory Format schema. Header records generated date, coverage % (read from coverage.json),
    tool + invocation, and the P1/P2/P3 rubric (P1 = correctness risk / missing guard / silent NaN;
    P2 = coverage gap / low hit rate; P3 = assertion density / assertion-free test).
    Generate the uncovered-path rows by running `cargo llvm-cov --package anofox-forecast --all-features --text --output-path /tmp/coverage_report.txt`
    (or `--show-missing-lines`) and transcribing the lowest-coverage functions as `| # | file | fn (line range) | missing invariant | priority | notes |` rows. Annotate any uncovered line inside a
    `#[cfg(target_arch = "wasm32")]` block as `// WASM-only` and EXCLUDE it from P1/P2 (Pitfall 5).
    Add an "Assertion-Free Tests" section using the RESEARCH assertion-density scan (tests with no
    `assert!`/`prop_assert!`) as P3 rows.
    Add a "Missing-Validation P1 Items" section that MUST include the four raw-vec global models 03-01
    deferred — `src/models/exponential/global_ets.rs`, `src/models/intermittent/global_croston.rs`,
    `src/models/theta/global_theta.rs`, `src/models/var.rs` — each with the risk ("NaN propagates silently
    into fitted params via unguarded &[Vec<f64>] input") and the recommended fix ("add per-series NaN/Inf scan
    before the estimation loop"). Add a P1 row for every model 03-01 recorded as PANICKING (from its SUMMARY /
    `// GAP:` annotations) and for any proptest counterexample 03-02 recorded. Every row must carry file +
    function + missing invariant so Phase 4 can consume it without rework.
  </action>
  <verify>
    <automated>test -f .planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md && grep -q global_ets .planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md && grep -q global_croston .planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md && grep -q global_theta .planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md && grep -q 'var.rs' .planning/phases/03-numerical-robustness-coverage-baseline/03-GAP-INVENTORY.md</automated>
  </verify>
  <acceptance_criteria>
    - The inventory file exists and contains the four deferred P1 raw-vec models: `grep -q global_ets ...03-GAP-INVENTORY.md && grep -q global_croston ...03-GAP-INVENTORY.md && grep -q global_theta ...03-GAP-INVENTORY.md && grep -q 'var.rs' ...03-GAP-INVENTORY.md` all succeed.
    - It records the coverage % from the baseline: `grep -qE 'coverage|%' ...03-GAP-INVENTORY.md` succeeds (a numeric coverage figure is present in the header).
    - Rows carry file + function + missing invariant (table with those columns): `grep -qi 'missing invariant' ...03-GAP-INVENTORY.md` succeeds.
    - It has a P1/P2/P3 priority rubric and at least one P1 row: `grep -q 'P1' ...03-GAP-INVENTORY.md` succeeds.
    - An assertion-free / assertion-density section is present: `grep -qiE 'assertion-free|assertion density' ...03-GAP-INVENTORY.md` succeeds.
  </acceptance_criteria>
  <done>03-GAP-INVENTORY.md is populated with uncovered-path rows, an assertion-density section, and the P1 raw-vec + panicking-model rows carried forward from 03-01/03-02 — structured for Phase 4 backlog consumption.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Lock the coverage baseline (commit coverage.json) and add the CI coverage hard-gate?</decision>
  <context>
    Committing `.planning/baselines/coverage.json` with `ratchet_floor_percent` and wiring
    `--fail-under-lines <floor>` into the ci.yml coverage: job is an up-only ratchet: once merged, every PR
    below the floor FAILS CI (SC-4), and the floor is only ever raised by a deliberate maintainer commit. This
    mirrors the Phase 2 accuracy.json baseline-lock decision checkpoint. The measured coverage % and computed
    floor from Task 1 are in coverage.json; the maintainer should confirm the ~1% ratchet margin and the
    `--package anofox-forecast` scope before this one-way-door gate lands.
  </context>
  <options>
    <option id="lock-as-measured">
      <name>Lock as measured (floor = coverage − ~1%, scoped to anofox-forecast)</name>
      <pros>Reproducible, up-only, matches the locked CONTEXT decision and Phase 2 precedent; blocks slow erosion immediately.</pros>
      <cons>A near-floor PR could fail CI until coverage is nudged up; the floor is deliberately conservative.</cons>
    </option>
    <option id="adjust-margin">
      <name>Adjust the ratchet margin or coverage scope before locking</name>
      <pros>Lets the maintainer widen the margin (e.g. −2%) or change `--package` scope if the measured number looks off.</pros>
      <cons>Requires re-running update_coverage.sh and re-reviewing before the gate lands.</cons>
    </option>
  </options>
  <resume-signal>Select: lock-as-measured or adjust-margin (and state the margin/scope). Executor commits coverage.json + proceeds to CI wiring only after selection.</resume-signal>
  <reversibility rating="one-way">Committing the baseline + adding a CI hard-gate is a one-way door: a PATCH-level ratchet that blocks merges below the floor and is only relaxed by a deliberate maintainer commit — mirrors the Phase 2 accuracy-lock.</reversibility>
</task>

<task type="auto">
  <name>Task 3: Extend the existing ci.yml coverage: job with the ratchet floor hard-gate</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (the coverage: job at lines 146-164: existing "Generate coverage" step `cargo llvm-cov --all-features --lcov --output-path lcov.info` and the "Upload to Codecov" step — the extension point is between them)
    - .planning/phases/03-numerical-robustness-coverage-baseline/03-RESEARCH.md (§CI Extension Pattern; Pitfall 3 — single-pass to avoid double instrumentation; Open Question 1 — verify single-pass locally before committing)
    - .planning/baselines/coverage.json (source of ratchet_floor_percent read at CI time)
  </read_first>
  <action>
    Extend the EXISTING `coverage:` job in `.github/workflows/ci.yml` — do NOT create a new workflow (per
    locked CONTEXT decision). Add a new step named "Enforce coverage floor" AFTER "Generate coverage" and
    BEFORE "Upload to Codecov". The step reads the floor with
    `FLOOR=$(jq '.coverage.ratchet_floor_percent' .planning/baselines/coverage.json)`, echoes it, and runs
    `cargo llvm-cov --package anofox-forecast --all-features --summary-only --fail-under-lines "$FLOOR"`
    (matching the baseline's `--package anofox-forecast --all-features` scope exactly so the CI number is
    comparable to the committed floor). `--fail-under-lines` exits 1 when coverage is below the floor, failing
    the job (SC-4). Keep the existing "Generate coverage" lcov step and the Codecov upload step unchanged so
    Codecov reporting is preserved. If local verification shows the single-pass `--lcov ... --fail-under-lines`
    form works (RESEARCH Open Question 1), the enforce step may instead be folded into the lcov step; default
    to the separate step form documented in RESEARCH §CI Extension Pattern.
    Only proceed with this task after the Task 2 checkpoint selection (coverage.json is committed).
  </action>
  <verify>
    <automated>grep -q 'Enforce coverage floor' .github/workflows/ci.yml && grep -q 'fail-under-lines' .github/workflows/ci.yml && grep -q 'ratchet_floor_percent' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - The coverage: job has a floor-enforcement step: `grep -q 'Enforce coverage floor' .github/workflows/ci.yml` succeeds.
    - CI reads the committed floor and passes it to cargo-llvm-cov: `grep -q 'ratchet_floor_percent' .github/workflows/ci.yml && grep -q -- '--fail-under-lines' .github/workflows/ci.yml` succeed.
    - The CI enforcement scope matches the baseline scope: `grep -q -- '--package anofox-forecast' .github/workflows/ci.yml` succeeds within the coverage job.
    - No new workflow file was created: `git diff --name-only` under `.github/workflows/` lists only `ci.yml` (no new *.yml).
    - The workflow still parses (no YAML break): `python3 -c "import yaml,sys; yaml.safe_load(open('.github/workflows/ci.yml'))"` exits 0.
  </acceptance_criteria>
  <done>The existing ci.yml coverage: job enforces the committed ratchet floor via --fail-under-lines; Codecov upload is preserved; no parallel workflow was created.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| committed baseline → CI gate | The `ratchet_floor_percent` value in coverage.json is trusted by CI as the merge-blocking threshold |
| local capture → committed artifact | update_coverage.sh output becomes the authoritative, human-reviewed baseline |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-03-08 | Tampering | Coverage floor silently lowered to pass a regressing PR | high | mitigate | Floor is a committed, human-reviewed artifact; decision checkpoint (Task 2) gates the lock; up-only ratchet documented — a lowered floor is a visible diff in coverage.json |
| T-03-09 | Repudiation | Feature-set mismatch makes the CI number non-comparable to the committed floor (false pass/fail) | medium | mitigate | Both baseline capture and CI enforcement use identical `--package anofox-forecast --all-features` scope, documented in coverage.invocation (Tasks 1 & 3) |
| T-03-10 | Denial of Service | Double instrumentation times out the coverage job | low | accept | RESEARCH Pitfall 3 single-pass option available; separate summary-only enforce run is fast; accepted with mitigation on standby |
</threat_model>

<verification>
- `bash scripts/update_coverage.sh` produces a valid coverage.json with a measured floor below measured coverage.
- `03-GAP-INVENTORY.md` contains the four P1 raw-vec models + any panicking-model / counterexample rows, with file + function + missing invariant columns and an assertion-density section.
- ci.yml coverage: job has an "Enforce coverage floor" step using `--fail-under-lines $ratchet_floor_percent` scoped to `--package anofox-forecast --all-features`; no new workflow created; YAML still parses.
- Decision checkpoint recorded before coverage.json was committed and CI hard-gated.
</verification>

<success_criteria>
- COVER-01: line-coverage baseline captured via cargo-llvm-cov, committed to
  .planning/baselines/coverage.json with provenance + ratchet floor; the existing ci.yml coverage: job
  enforces the floor and FAILS below it (SC-4).
- COVER-02: 03-GAP-INVENTORY.md lists uncovered paths and assertion-free tests with file + function +
  missing invariant, including the P1 raw-vec + panicking-model rows carried from 03-01/03-02, structured
  as direct Phase 4 backlog input (SC-5).
</success_criteria>

<output>
Create `.planning/phases/03-numerical-robustness-coverage-baseline/03-03-SUMMARY.md` when done.
Record in the SUMMARY: the measured coverage %, the committed ratchet floor, the coverage scope
(`--package anofox-forecast --all-features`), the decision-checkpoint outcome (lock-as-measured vs
adjusted margin/scope), and a count of P1/P2/P3 rows filed in the gap inventory for Phase 4.
</output>
</content>
