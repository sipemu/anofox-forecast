---
phase: 02-accuracy-harness-statistical-methodology
plan: 04
type: execute
wave: 4
depends_on: ["02-01", "02-02", "02-03"]
files_modified:
  - crates/anofox-bench-harness/tests/accuracy.rs
  - .planning/baselines/accuracy.json
  - .github/workflows/accuracy.yml
autonomous: false
requirements: [ACCUR-07, ACCUR-08]
user_setup:
  - service: local-dataset-corpus
    why: "The ACCUR-08 anchor validation and accuracy.json capture read the full M3 corpus; run on a maintainer machine with ANOFOX_DATASET_DIR set (Phase 1 rule: baselines captured locally, never in CI)."
    env_vars:
      - name: ANOFOX_DATASET_DIR
        source: "./validation/data"

estimate:
  tokens: 70000
  raw_tokens: 35000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "An ACCUR-08 anchor test asserts the whole-corpus AutoETS M3-monthly MASE is within 0.02 of the published statsforecast 0.93 reference BEFORE accuracy.json is written/committed (ACCUR-08)"
    - "accuracy.json carries per-frequency (Yearly/Quarterly/Monthly) Naive2 + AutoETS metrics and a provenance block, and is committed only after the anchor passes — never auto-written by CI (ACCUR-07, ACCUR-08, D-05)"
    - "accuracy.yml is workflow_dispatch-only, has contents:read permissions, sets ANOFOX_DATASET_DIR, runs the accuracy tests, and never gates a PR merge (MEAS-03)"
  artifacts:
    - ".planning/baselines/accuracy.json"
    - ".github/workflows/accuracy.yml"
    - "crates/anofox-bench-harness/tests/accuracy.rs (anchor + JSON emit)"
  key_links:
    - "ACCUR-08 anchor pass → gate that must succeed before accuracy.json is locked (one-way decision, checkpoint before lock)"
    - "per-frequency FrequencyResult (Plan 02) → accuracy.json rows (this plan serializes them)"
    - "accuracy.yml workflow_dispatch-only → never blocks PR merges (Phase 1 CI-read-only rule)"
---

<objective>
Close the phase: validate the whole-corpus AutoETS M3-monthly MASE against the published statsforecast 0.93 anchor (ACCUR-08), and ONLY after that anchor passes, lock the per-frequency `accuracy.json` baseline (Naive2 + AutoETS over M3 Y/Q/M, D-05) with a provenance block — then add the `workflow_dispatch`-only `accuracy.yml` workflow that reads the harness but never gates PRs (MEAS-03).

Purpose: make the accuracy numbers trustworthy (anchored) and the baseline auditable and non-regressing, without ever letting CI overwrite baselines or block merges.
Output: the anchor test, the committed `accuracy.json`, and `accuracy.yml`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-CONTEXT.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-PATTERNS.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-02-SUMMARY.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-03-SUMMARY.md
@crates/anofox-bench-harness/tests/accuracy.rs
@crates/anofox-bench-harness/src/baseline.rs
@.github/workflows/bench.yml
@.planning/baselines/statsforecast_reference.json
</context>

## Artifacts this plan produces

- `crates/anofox-bench-harness/tests/accuracy.rs` — `accur08_anchor_m3_monthly_autoets` test + a JSON-emit helper writing `accuracy.json` (guarded behind an env flag so ordinary `cargo test` never overwrites the baseline).
- `.planning/baselines/accuracy.json` — locked per-frequency Naive2 + AutoETS baseline with provenance (D-05).
- `.github/workflows/accuracy.yml` — `workflow_dispatch`-only accuracy workflow (MEAS-03).

<tasks>

<task type="auto">
  <name>Task 1: ACCUR-08 whole-corpus anchor test + accuracy.json emit helper (tests/accuracy.rs)</name>
  <files>crates/anofox-bench-harness/tests/accuracy.rs</files>
  <read_first>
    - crates/anofox-bench-harness/tests/accuracy.rs (run_accuracy_harness / FrequencyResult from Plan 02 — reuse the aggregate)
    - crates/anofox-bench-harness/src/baseline.rs (ProvenanceFingerprint — reuse for accuracy.json provenance)
    - .planning/baselines/statsforecast_reference.json (the pinned reference monthly MASE from Plan 03)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-PATTERNS.md (section "tests/accuracy.rs" — accur08_anchor assertion style; ".planning/baselines/accuracy.json" schema)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md (Accuracy JSON Schema; anti-pattern "committing accuracy.json before ACCUR-08 passes")
  </read_first>
  <action>
    Add `accur08_anchor_m3_monthly_autoets` to `tests/accuracy.rs`: run `run_accuracy_harness()`; env unset → clean skip. Read the reference monthly MASE from `.planning/baselines/statsforecast_reference.json` (fall back to the literal 0.93 anchor if the fixture read is scoped out — but prefer the fixture value). Assert `results["monthly"].autoets_mase.is_finite()` and `(autoets_mase - reference_monthly_mase).abs() <= 0.02` with a message citing ACCUR-08 and pointing at the D-03 fix / training-denominator if it fails.

    Add a JSON-emit helper `fn emit_accuracy_json()` that serializes the per-frequency `FrequencyResult` map into the accuracy.json schema (per RESEARCH.md: `{ provenance, datasets: { M3: { monthly/quarterly/yearly: { n_series, horizon, models: { AutoETS: {mase,smape,rmse,mae}, Naive2: {...} } } } } }`) plus MSIS/coverage where present, attaching a `ProvenanceFingerprint`. CRITICAL: this helper writes the file ONLY when an explicit env flag is set (e.g. `ANOFOX_WRITE_ACCURACY_BASELINE=1`) AND the anchor assertion has passed — so an ordinary `cargo test` run NEVER overwrites the committed baseline (Phase 1 CI-read-only discipline). Structure it so the anchor test runs first / the emit is refused if the monthly anchor is outside tolerance.

    Do NOT commit accuracy.json in this task — Task 2 (behind the checkpoint) performs the actual capture + lock. This task only lands the anchor test and the guarded emit helper.
  </action>
  <acceptance_criteria>
    - `tests/accuracy.rs` contains `accur08_anchor_m3_monthly_autoets` asserting monthly AutoETS MASE within 0.02 of the reference (≈0.93).
    - `cargo test -p anofox-bench-harness --test accuracy` passes with env UNSET (clean skip).
    - The emit helper writes accuracy.json only when `ANOFOX_WRITE_ACCURACY_BASELINE` is set AND the anchor passed (grep: env-flag guard + a refuse-on-out-of-tolerance branch).
    - Running plain `cargo test` (no write flag) leaves `.planning/baselines/accuracy.json` unchanged (git status clean for that path after the test run).
    - `cargo clippy -p anofox-bench-harness --tests -- -D warnings` clean; `cargo fmt --check` passes.
  </acceptance_criteria>
  <precondition>ANOFOX_DATASET_DIR points at the M3 corpus AND statsforecast_reference.json (Plan 03) carries the trusted ≈0.93 monthly reference; if Plan 03's regeneration was deferred, this anchor is blocked until the fixture is trusted (recorded in Plan 03 SUMMARY).</precondition>
  <verify>
    <automated>cargo test -p anofox-bench-harness --test accuracy 2>&1 | tail -15 && git status --porcelain .planning/baselines/accuracy.json 2>&1 | tail -3</automated>
  </verify>
  <reversibility rating="reversible">Test + guarded emit helper; no baseline written yet.</reversibility>
  <done>The ACCUR-08 anchor test and a write-flag-guarded accuracy.json emit helper exist; plain test runs never overwrite the baseline.</done>
</task>

<task type="checkpoint:decision" gate="blocking">
  <decision>Lock .planning/baselines/accuracy.json as the committed M3 Naive2+AutoETS accuracy baseline</decision>
  <context>
    Committing accuracy.json is a one-way decision (REVERSIBILITY_GATES): once locked, it becomes the regression anchor future phases measure against, so it must only be captured after the ACCUR-08 anchor validates. The anchor requires (a) ANOFOX_DATASET_DIR set to the M3 corpus and (b) a trusted statsforecast_reference.json (Plan 03, pinned provenance) reporting AutoETS M3-monthly MASE ≈ 0.93. If Plan 03's Python regeneration was deferred (pinned env unavailable), the reference is not yet trusted and the lock MUST NOT proceed.
  </context>
  <options>
    <option id="lock-now">
      <name>Anchor passed — capture and lock accuracy.json now</name>
      <pros>Delivers ACCUR-07/ACCUR-08 baseline; enables Phase 4 accuracy-delta measurement</pros>
      <cons>None if the anchor is within ±0.02 of 0.93 on the trusted reference</cons>
    </option>
    <option id="defer-lock">
      <name>Defer — reference fixture not yet trusted / anchor outside tolerance</name>
      <pros>Avoids locking an unvalidated number; keeps the baseline honest</pros>
      <cons>ACCUR-08 remains open until the pinned fixture is regenerated and the anchor lands in tolerance</cons>
    </option>
  </options>
  <resume-signal>Confirm the ANOFOX_DATASET_DIR-run anchor value (report the observed M3-monthly AutoETS MASE) and select: lock-now or defer-lock</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Capture + lock accuracy.json, and add workflow_dispatch-only accuracy.yml</name>
  <files>.planning/baselines/accuracy.json, .github/workflows/accuracy.yml</files>
  <read_first>
    - crates/anofox-bench-harness/tests/accuracy.rs (the emit helper + anchor from Task 1)
    - crates/anofox-bench-harness/src/baseline.rs (ProvenanceFingerprint shape for the accuracy.json provenance block)
    - .github/workflows/bench.yml (permissions, toolchain, cache steps to mirror; contrast: bench.yml uses push/PR, accuracy.yml must be workflow_dispatch-only)
    - .github/workflows/wasm-size.yml (Phase 1 workflow_dispatch/quoted-'on' precedent)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-PATTERNS.md (section ".github/workflows/accuracy.yml" — trigger, permissions, toolchain, job step)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md (anti-pattern "regenerating the reference fixture in CI")
  </read_first>
  <action>
    Gated on the checkpoint selecting `lock-now`:

    Capture the baseline by running the emit helper with the dataset env + write flag on the maintainer machine (`ANOFOX_DATASET_DIR=./validation/data ANOFOX_WRITE_ACCURACY_BASELINE=1 cargo test -p anofox-bench-harness --test accuracy accur08 ...`). Confirm `.planning/baselines/accuracy.json` contains: a `provenance` block (git_sha, timestamp_iso, rustc_version, host_cpu, host_os, active_features), per-frequency M3 monthly/quarterly/yearly entries each with `n_series`, `horizon`, and `models.AutoETS`/`models.Naive2` metric rows (mase/smape/rmse/mae; monthly additionally msis/coverage). Verify the monthly AutoETS mase is ≈0.93 (the anchor) before committing. Include a `note` field in provenance recording the D-03 MASE-fix before/after and the MSIS period-1 convention caveat (from Plan 02).

    If the checkpoint selected `defer-lock`: do NOT write accuracy.json; record the blocker in the SUMMARY and still deliver accuracy.yml (below) so the workflow exists for when the lock lands.

    Create `.github/workflows/accuracy.yml`: name "Accuracy Harness"; trigger `workflow_dispatch` ONLY (use a quoted `'on':` key per the Phase 1 wasm-size.yml precedent to avoid YAML boolean coercion; NEVER add push/pull_request — MEAS-03: never gates PR merges). `permissions: contents: read`. Steps mirror bench.yml: `actions/checkout@v4`, `dtolnay/rust-toolchain@stable`, `Swatinem/rust-cache@v2`, then a run step setting `ANOFOX_DATASET_DIR: ${{ github.workspace }}/validation/data` and running `cargo test -p anofox-bench-harness --test accuracy -- --nocapture`. The workflow must NOT run Python / regenerate the reference fixture and must NOT write any baseline (read-only; capture is local-only per Phase 1 rule).
  </action>
  <acceptance_criteria>
    - `.github/workflows/accuracy.yml` exists, triggers on `workflow_dispatch` only, and contains NO `push:` or `pull_request:` trigger (grep confirms absence).
    - accuracy.yml has `permissions: contents: read` and runs `cargo test -p anofox-bench-harness --test accuracy`, and contains no Python/statsforecast run step and no baseline-write step.
    - If lock-now: `.planning/baselines/accuracy.json` exists with a provenance block, three per-frequency M3 entries, AutoETS + Naive2 rows, monthly AutoETS mase within ±0.02 of 0.93. If defer-lock: accuracy.json is not created and the SUMMARY records the blocker.
    - `python -c` / jq check confirms accuracy.json (when present) is valid JSON with `provenance` and `datasets.M3.monthly/quarterly/yearly` keys.
    - No CI workflow (accuracy.yml included) overwrites any file under .planning/baselines/.
  </acceptance_criteria>
  <precondition>The checkpoint:decision resolved (lock-now requires the ACCUR-08 anchor within tolerance on a trusted reference); ANOFOX_DATASET_DIR set for the capture run.</precondition>
  <verify>
    <automated>test -f .github/workflows/accuracy.yml && ! grep -qE '^\s*(push|pull_request):' .github/workflows/accuracy.yml && echo "accuracy.yml workflow_dispatch-only OK"; test -f .planning/baselines/accuracy.json && python -c "import json; d=json.load(open('.planning/baselines/accuracy.json')); assert 'provenance' in d and 'M3' in d['datasets']; print('accuracy.json OK')" || echo "accuracy.json deferred (see SUMMARY)"</automated>
  </verify>
  <reversibility rating="one-way">Committing accuracy.json establishes the regression anchor future phases measure against; the preceding checkpoint:decision gates the lock, and the ACCUR-08 ≈0.93 validation must pass first.</reversibility>
  <done>accuracy.yml is workflow_dispatch-only and read-only; accuracy.json is locked with provenance and per-frequency rows (anchor within tolerance) — or explicitly deferred with a recorded blocker.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| harness → committed baseline | accuracy.json is written locally (never by CI) and becomes the regression anchor |
| CI workflow → repo | accuracy.yml runs the harness read-only; must not write baselines or gate merges |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-LOCK | Tampering (unvalidated anchor) | accuracy.json lock | medium | mitigate | Emit helper writes only behind an explicit env flag AND a passing ACCUR-08 anchor; a checkpoint:decision gates the lock so no unvalidated number is committed |
| T-02-CIGATE | Denial of Service (blocked merges) | accuracy.yml trigger | medium | mitigate | `workflow_dispatch`-only trigger + `contents: read` permissions guarantee the workflow never gates a PR merge (MEAS-03); verified by grep absence of push/pull_request |
| T-02-CIWRITE | Tampering | accuracy.yml baseline overwrite | low | mitigate | Workflow runs tests read-only with no write flag; capture is a local-only maintainer step (Phase 1 CI-read-only rule) |

No high-severity threats: no auth/network/session/crypto surface; CI is read-only and manually triggered. V2/V3/V4/V6 not applicable.
</threat_model>

<verification>
- `cargo test -p anofox-bench-harness --test accuracy` passes with env unset (clean skips); with `ANOFOX_DATASET_DIR=./validation/data` the ACCUR-08 anchor asserts monthly AutoETS MASE within ±0.02 of 0.93.
- accuracy.yml is workflow_dispatch-only with contents:read, runs the harness, and contains no push/pull_request trigger, no Python step, and no baseline write.
- accuracy.json (when locked) is valid JSON with a provenance block and three per-frequency M3 entries; plain `cargo test` never overwrites it.
- `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` passes.
</verification>

<success_criteria>
The ACCUR-08 anchor validates the M3-monthly AutoETS MASE against the published 0.93 reference before lock; the per-frequency `accuracy.json` baseline is committed (gated by an explicit decision + passing anchor) with provenance; and the `workflow_dispatch`-only `accuracy.yml` reads the harness without ever gating a PR merge.
</success_criteria>

<output>
Create `.planning/phases/02-accuracy-harness-statistical-methodology/02-04-SUMMARY.md` when done.
</output>
