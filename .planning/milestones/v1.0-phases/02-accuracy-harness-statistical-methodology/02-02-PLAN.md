---
phase: 02-accuracy-harness-statistical-methodology
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - crates/anofox-bench-harness/src/naive2.rs
  - crates/anofox-bench-harness/src/lib.rs
  - crates/anofox-bench-harness/tests/accuracy.rs
autonomous: true
requirements: [ACCUR-02, ACCUR-05, ACCUR-06, ACCUR-07]
user_setup:
  - service: local-dataset-corpus
    why: "The full per-frequency stratified run reads M3 Yearly/Quarterly/Monthly TSF files; without ANOFOX_DATASET_DIR the integration test skips."
    env_vars:
      - name: ANOFOX_DATASET_DIR
        source: "./validation/data (vendored corpus already present)"

estimate:
  tokens: 82000
  raw_tokens: 41000
  tasks: 3
  confidence: med

must_haves:
  truths:
    - "Naive2 selects SeasonalNaive when the training series passes a 90%-confidence ACF test at the seasonal lag (|acf| > 1.645/sqrt(n)), else falls back to Naive (D-08, ACCUR-06)"
    - "The harness produces a per-frequency stratified result table for M3 Yearly/Quarterly/Monthly — never a single cross-frequency aggregate — for both Naive2 and AutoETS, with MASE (training denominator), sMAPE, RMSE, MAE and interval coverage/MSIS (ACCUR-05, ACCUR-07)"
    - "No NaN or Inf silently enters any per-frequency aggregate: non-finite per-series metrics are filtered and the excluded count is logged before averaging (ACCUR-03 harness layer)"
    - "Every evaluation fold asserts train_end < test_start; fitted_values() is never used as an out-of-sample proxy (ACCUR-02)"
  artifacts:
    - "crates/anofox-bench-harness/src/naive2.rs"
    - "crates/anofox-bench-harness/tests/accuracy.rs (extended: per-frequency run)"
  key_links:
    - "Naive2 → the accuracy reference model compared against AutoETS (consumed by Plans 03/04)"
    - "per-frequency FrequencyResult table → accuracy.json schema (consumed by Plan 04)"
    - "NaN-guarded aggregation → trustworthy anchor number (consumed by Plan 04 ACCUR-08)"
---

<objective>
Expand the tracer into the full per-frequency accuracy run: implement Naive2 (ACF-gated seasonal/non-seasonal reference, D-07/D-08), wire the complete reused metric set (sMAPE/RMSE/MAE/MSIS/coverage from `src/utils/metrics.rs` per D-01, plus the training-denominator MASE from Plan 01), and produce a per-frequency stratified result table over M3 Yearly/Quarterly/Monthly for both Naive2 and AutoETS — with NaN/Inf-guarded aggregation.

Purpose: deliver genuine per-frequency stratification (ACCUR-07) with a validated reference baseline (ACCUR-06) that Plan 04 locks into `accuracy.json`.
Output: `naive2.rs` and the expanded `tests/accuracy.rs` producing a `HashMap<Frequency, FrequencyResult>` for the two models.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-CONTEXT.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-PATTERNS.md
@.planning/phases/02-accuracy-harness-statistical-methodology/02-01-SUMMARY.md
@crates/anofox-bench-harness/src/loader.rs
@crates/anofox-bench-harness/tests/accuracy.rs
@src/utils/metrics.rs
@src/models/baseline/mod.rs
</context>

## Artifacts this plan produces

- `crates/anofox-bench-harness/src/naive2.rs` — `pub struct Naive2 { seasonal_period, inner }`, `enum Naive2Inner { Seasonal(SeasonalNaive), Random(Naive) }`, `Naive2::new(period)`, `Naive2::fit(&mut self, train)`, `Naive2::predict(&self, horizon)`; `fn acf_at_lag(series, lag) -> f64`.
- `crates/anofox-bench-harness/src/lib.rs` — `pub mod naive2;`.
- `crates/anofox-bench-harness/tests/accuracy.rs` — extended with `FrequencyResult`, the per-frequency run function, and tests `naive2_seasonal_gate`, `per_frequency_stratification`.

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Naive2 model — ACF-gated seasonal/non-seasonal reference (naive2.rs, lib.rs)</name>
  <files>crates/anofox-bench-harness/src/naive2.rs, crates/anofox-bench-harness/src/lib.rs</files>
  <read_first>
    - src/models/baseline/mod.rs and src/models/baseline/{naive.rs, seasonal_naive.rs} (Naive::new(), SeasonalNaive::new(period), their fit/predict via the Forecaster trait)
    - src/models/traits.rs (the Forecaster trait: fit(&mut self, &TimeSeries) -> Result, predict(&self, horizon) -> Result<Forecast>)
    - crates/anofox-bench-harness/tests/accuracy.rs (the make_ts_from_slice helper landed by Plan 01 — reuse the same synthetic-timestamp construction)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-PATTERNS.md (section "naive2.rs" — module doc, imports, struct/enum, ACF helper, TimeSeries construction)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md (Pattern 4, Assumption A1 — 1.645/sqrt(n) critical value)
  </read_first>
  <behavior>
    - Test `naive2_seasonal_gate` (seasonal): a strongly seasonal training series (e.g. `[10,20,30,40]` repeated with period 4) passes the ACF test → Naive2 uses SeasonalNaive → predict(4) repeats the last season `[10,20,30,40]`.
    - Test `naive2_seasonal_gate` (flat): a flat/random series with no seasonal autocorrelation → Naive2 uses Naive → predict(4) carries the last value forward `[last,last,last,last]`.
    - `acf_at_lag` on a pure period-4 sinusoid returns a value with `abs() > 1.645/sqrt(n)`; on i.i.d.-like noise returns `abs()` below that threshold for adequately long n.
  </behavior>
  <action>
    Create `crates/anofox-bench-harness/src/naive2.rs`; add `pub mod naive2;` to `crates/anofox-bench-harness/src/lib.rs` (update the doc-comment module list, ACCUR-06/D-07).

    Import `anofox_forecast::models::baseline::{Naive, SeasonalNaive}`, `anofox_forecast::models::Forecaster`, `anofox_forecast::core::TimeSeries`, `anofox_forecast::error::ForecastError`, and chrono for timestamp construction.

    Define `pub struct Naive2 { seasonal_period: usize, inner: Naive2Inner }` and `enum Naive2Inner { Seasonal(SeasonalNaive), Random(Naive) }`. `pub fn new(seasonal_period: usize) -> Self` initializes `inner: Naive2Inner::Random(Naive::new())`.

    Implement `fn acf_at_lag(series: &[f64], lag: usize) -> f64` per the PATTERNS pattern: mean-centered numerator `sum_{t=lag}^{n-1} (x_t - xbar)(x_{t-lag} - xbar)` over denominator `sum (x_t - xbar)^2`; return `0.0` when `n <= lag` or denominator is zero.

    Implement `pub fn fit(&mut self, train: &[f64]) -> Result<(), ForecastError>`: compute `acf = acf_at_lag(train, self.seasonal_period)`, `critical = 1.645 / (train.len() as f64).sqrt()` (90%-confidence ACF test, D-08/A1). If `acf.abs() > critical` and `self.seasonal_period >= 2` and `train.len() > self.seasonal_period`, fit a `SeasonalNaive::new(self.seasonal_period)` on `make_ts_from_slice(train)` and store `Naive2Inner::Seasonal`; else fit a `Naive::new()` and store `Naive2Inner::Random`. Reuse the same synthetic-timestamp `make_ts_from_slice` construction as accuracy.rs (duplicate the small helper here or expose it — keep it fmt-clean and clippy-clean).

    Implement `pub fn predict(&self, horizon: usize) -> Result<Vec<f64>, ForecastError>` delegating to the inner model's `predict(horizon)` and extracting the univariate point-forecast vector from the returned `Forecast` (read `src/models/traits.rs` / the Forecast type for the exact accessor — return the first dimension's values).

    Add `#[cfg(test)]` unit tests implementing the three `<behavior>` cases. These must NOT require ANOFOX_DATASET_DIR.

    No customer/client names anywhere; module doc comment per the PATTERNS convention.
  </action>
  <acceptance_criteria>
    - `crates/anofox-bench-harness/src/naive2.rs` exists; `lib.rs` declares `pub mod naive2;`.
    - `cargo test -p anofox-bench-harness naive2::` passes (all three behavior tests) without the dataset env var.
    - The seasonal gate uses critical value `1.645 / sqrt(n)` (grep: `1.645` present in naive2.rs).
    - On a period-4 repeating training series, `Naive2::predict(4)` reproduces the last season; on a flat series it carries the last value forward.
    - `cargo clippy -p anofox-bench-harness -- -D warnings` clean; `cargo fmt --check` passes.
  </acceptance_criteria>
  <verify>
    <automated>cargo test -p anofox-bench-harness naive2:: 2>&1 | tail -20</automated>
  </verify>
  <reversibility rating="reversible">New harness-only reference model; the ACF threshold is documented (A1) and revisable if the ACCUR-08 anchor demands recalibration.</reversibility>
  <done>Naive2 fits SeasonalNaive vs Naive by a 90%-confidence ACF test and predicts the correct branch; unit tests pass offline.</done>
</task>

<task type="auto">
  <name>Task 2: Per-frequency stratified run over M3 Y/Q/M for Naive2 + AutoETS (tests/accuracy.rs)</name>
  <files>crates/anofox-bench-harness/tests/accuracy.rs</files>
  <read_first>
    - crates/anofox-bench-harness/tests/accuracy.rs (the Plan 01 tracer + make_ts_from_slice helper — extend, do not rewrite the tracer)
    - crates/anofox-bench-harness/src/loader.rs (load_m3, DatasetSeries.frequency/horizon, mase_scale, dataset_dir_from_env)
    - crates/anofox-bench-harness/src/naive2.rs (Naive2 API from Task 1)
    - src/utils/metrics.rs (standalone reused metrics: smape, rmse, mae — verify exact fn names/signatures at lines ~172–357, D-01)
    - src/utils/cross_validation.rs (CvFoldGenerator single-fold split as used by the tracer)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-PATTERNS.md (per-frequency loop, NaN-guard aggregate, assertion style)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md (Pitfall 1, Pitfall 3, anti-pattern "cross-frequency aggregation")
  </read_first>
  <action>
    Extend `tests/accuracy.rs` (keep the Plan 01 tracer test intact). Define `pub struct FrequencyResult { pub frequency: String, pub n_series: usize, pub horizon: usize, pub autoets_mase: f64, pub naive2_mase: f64, pub autoets_smape: f64, pub naive2_smape: f64, pub autoets_rmse: f64, pub autoets_mae: f64, pub skipped_nonfinite: usize }` (the exact metric fields Plan 04 serializes into accuracy.json).

    Implement `fn run_accuracy_harness() -> std::collections::HashMap<String, FrequencyResult>`: env-gate first (`dataset_dir_from_env()` → return empty map on None). `load_m3(dir)` gives all M3 series tagged by frequency. Map each `DatasetSeries.frequency` string to its seasonal period: monthly→12, quarterly→4, yearly→1. Group series by frequency.

    For each frequency group, for each series with enough data (`values.len() > horizon + period + 2`):
    - Build the SAME single expanding-window fold as the tracer (`CvFoldGenerator` with `horizon = series.horizon`, `min_initial_window = len - horizon`, Expanding, n_folds(1)); assert `fold.train_end <= fold.test_start` on every fold (ACCUR-02). Never call `fitted_values()`.
    - Slice train/test; fit + predict both `Naive2::new(period)` and `AutoETS::auto()`.
    - MASE via `mase_scale(train, period)` training denominator (Pitfall 1). sMAPE/RMSE/MAE via the reused `src/utils/metrics.rs` standalone functions on (test, pred) (D-01).
    - Collect per-series metrics into vectors.

    Aggregate per frequency with the NaN-guard: filter each metric vector to `is_finite()` values, count `skipped_nonfinite = original_len - finite_len`, `eprintln!` a WARNING with the skipped count when > 0, then average the finite values. Populate `FrequencyResult` and insert under the frequency key. NEVER average across frequencies (ACCUR-07).

    Add test `per_frequency_stratification`: run the harness; if the map is empty, print the skip message and return (env unset). Otherwise assert the map contains separate `"monthly"`, `"quarterly"`, `"yearly"` keys, each with `n_series > 0`, each `autoets_mase.is_finite()` and `naive2_mase.is_finite()`, and assert no cross-frequency key exists (e.g. no `"all"`/`"combined"` aggregate).
  </action>
  <acceptance_criteria>
    - `cargo test -p anofox-bench-harness --test accuracy` passes with env UNSET (all tests skip cleanly, exit 0).
    - With `ANOFOX_DATASET_DIR=./validation/data`, `per_frequency_stratification` asserts three separate frequency entries (monthly/quarterly/yearly), each with finite Naive2 and AutoETS MASE.
    - The aggregation path contains an `is_finite()` filter and logs a skipped-count WARNING before averaging (grep: `is_finite` and `skipped` present in accuracy.rs aggregation).
    - MASE is computed via `mase_scale` (training denominator); `ForecastMetrics::compute` is absent from accuracy.rs.
    - `fitted_values()` is absent from accuracy.rs.
    - The per-frequency loop asserts `fold.train_end <= fold.test_start`.
    - `cargo clippy -p anofox-bench-harness --tests -- -D warnings` clean; `cargo fmt --check` passes.
  </acceptance_criteria>
  <precondition>ANOFOX_DATASET_DIR points at the M3 corpus for the non-skipped run; unset → clean skip (user_setup).</precondition>
  <verify>
    <automated>cargo test -p anofox-bench-harness --test accuracy 2>&1 | tail -25 && ANOFOX_DATASET_DIR=./validation/data cargo test -p anofox-bench-harness --test accuracy per_frequency_stratification -- --nocapture 2>&1 | tail -25</automated>
  </verify>
  <reversibility rating="reversible">Test-only expansion; FrequencyResult fields are revisable before the Plan 04 lock.</reversibility>
  <done>The harness produces a per-frequency (Y/Q/M) stratified table for Naive2 + AutoETS with reused metrics and NaN-guarded aggregation; the stratification test asserts three separate frequency buckets with finite metrics.</done>
</task>

<task type="auto">
  <name>Task 3: MSIS + empirical interval-coverage wiring for AutoETS prediction intervals (tests/accuracy.rs)</name>
  <files>crates/anofox-bench-harness/tests/accuracy.rs</files>
  <read_first>
    - src/utils/metrics.rs (msis at lines ~307–341, coverage at ~348–357 — exact signatures and the interval-input shape, D-01/ACCUR-05)
    - src/models/traits.rs and the AutoETS predict-with-intervals API (predict_with_intervals or equivalent — verify the method name and the Forecast lower/upper accessors)
    - crates/anofox-bench-harness/tests/accuracy.rs (the per-frequency run from Task 2 — add interval metrics into FrequencyResult flow)
    - .planning/phases/02-accuracy-harness-statistical-methodology/02-RESEARCH.md (Pitfall 4, Assumption A4 — MSIS period-1 vs seasonal convention; ACCUR-05)
  </read_first>
  <action>
    Add MSIS and empirical interval-coverage to the M3-monthly evaluation only (monthly has the richest intervals and is the ACCUR-08 anchor frequency; keep interval metrics scoped to monthly to bound runtime, documenting this in a `//` comment). Extend `FrequencyResult` with `pub autoets_msis: Option<f64>` and `pub autoets_coverage: Option<f64>` (Option so non-interval frequencies stay None rather than NaN).

    For each M3-monthly series: obtain AutoETS prediction intervals via the library's interval API (verify exact method; e.g. `predict_with_intervals(horizon, level)` returning a `Forecast` with lower/upper). Feed (test actuals, lower, upper, and the training-slice scale as required by the reused `msis()` signature) into `src/utils/metrics.rs::msis` and `::coverage` (D-01 — reuse, do not reimplement). Aggregate MSIS/coverage with the same `is_finite()` guard.

    IMPORTANT convention note (A4/Pitfall 4): the reused `msis()` scales by period-1 first differences, which diverges from the M4 seasonal-difference MSIS. Do NOT claim the reported MSIS equals published M4 MSIS. Add a `//` comment documenting the convention and record it in the SUMMARY so Plan 04's provenance block carries the caveat. Do NOT gate ACCUR-08 on MSIS — the anchor is MASE only.

    Add test `msis_coverage_present_monthly`: with the dataset env set, assert `results["monthly"].autoets_msis` is `Some(finite)` and `autoets_coverage` is `Some(c)` with `0.0 <= c <= 1.0`; env unset → clean skip.
  </action>
  <acceptance_criteria>
    - `FrequencyResult` carries `autoets_msis: Option<f64>` and `autoets_coverage: Option<f64>`.
    - MSIS and coverage are computed via the reused `src/utils/metrics.rs::msis` / `::coverage` (grep: `msis` and `coverage` referenced from the harness metrics import, not reimplemented in accuracy.rs).
    - With `ANOFOX_DATASET_DIR=./validation/data`, `msis_coverage_present_monthly` asserts monthly MSIS is `Some(finite)` and coverage ∈ [0,1].
    - A `//` comment in accuracy.rs documents the period-1-vs-seasonal MSIS convention (A4); ACCUR-08 anchor is not gated on MSIS.
    - `cargo test -p anofox-bench-harness --test accuracy` passes with env UNSET (clean skip).
    - `cargo clippy -p anofox-bench-harness --tests -- -D warnings` clean; `cargo fmt --check` passes.
  </acceptance_criteria>
  <precondition>ANOFOX_DATASET_DIR points at the M3 corpus for the non-skipped run; the AutoETS interval API (predict_with_intervals or equivalent) exists in the library (verify in read_first).</precondition>
  <verify>
    <automated>ANOFOX_DATASET_DIR=./validation/data cargo test -p anofox-bench-harness --test accuracy msis_coverage_present_monthly -- --nocapture 2>&1 | tail -20</automated>
  </verify>
  <reversibility rating="reversible">Test-only; interval metrics are additive Option fields.</reversibility>
  <done>MSIS and empirical coverage are computed for M3-monthly AutoETS intervals via the reused library metrics, guarded and documented for the period-1 convention, with the anchor still MASE-only.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| filesystem → harness | M3 TSF files parsed by the loader (Plan 01) feed model fitting |
| library API → harness | Naive/SeasonalNaive/AutoETS outputs consumed for metric computation |

## STRIDE Threat Register (ASVS L1; block on high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-NAN2 | Tampering (correctness) | accuracy.rs per-frequency aggregation | medium | mitigate | `is_finite()` filter + logged skipped-count before averaging prevents silent NaN/Inf in any frequency bucket (ACCUR-03 harness layer) |
| T-02-XFREQ | Information Disclosure (misleading metric) | accuracy.rs aggregation | low | mitigate | Per-frequency stratification enforced by test `per_frequency_stratification`; no cross-frequency aggregate key permitted (ACCUR-07) |
| T-02-ACF | Tampering (correctness) | naive2.rs ACF gate threshold | low | accept | 1.645/sqrt(n) is the documented statsforecast-canonical threshold (A1); mis-selection surfaces as an ACCUR-08 anchor mismatch caught in Plan 04, not a silent fault |

No high-severity threats: harness-only, no auth/network/session/crypto surface. V2/V3/V4/V6 ASVS categories not applicable.
</threat_model>

<verification>
- `cargo test -p anofox-bench-harness` (naive2 + loader unit tests) passes offline.
- `cargo test -p anofox-bench-harness --test accuracy` passes with env unset (clean skips) and, with `ANOFOX_DATASET_DIR=./validation/data`, produces three per-frequency buckets with finite Naive2/AutoETS MASE plus monthly MSIS/coverage.
- `cargo clippy --all-targets -- -D warnings` clean; `cargo fmt --check` passes.
</verification>

<success_criteria>
Naive2 (ACCUR-06) and per-frequency stratified reporting (ACCUR-07) exist with reused metrics (ACCUR-04/05) and NaN-guarded aggregation, producing the `FrequencyResult` table Plan 04 will lock into `accuracy.json`.
</success_criteria>

<output>
Create `.planning/phases/02-accuracy-harness-statistical-methodology/02-02-SUMMARY.md` when done.
</output>
