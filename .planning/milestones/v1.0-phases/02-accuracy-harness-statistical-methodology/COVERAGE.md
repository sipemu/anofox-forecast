# API Coverage — Phase 02

No external API integration: this phase loads local vendored dataset files (`validation/data/*.tsf`, JSON) and compares against a committed statsforecast *reference fixture* (a static JSON number from literature/pinned regeneration), not any live network API/SDK/service. No coverage matrix applies.
