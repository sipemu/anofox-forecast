---
phase: 01-measurement-infrastructure-compute-baselines
plan: 03
type: execute
wave: 3
depends_on: ["01-01", "01-02"]
files_modified:
  - Cargo.toml
  - crates/anofox-bench-harness/Cargo.toml
  - benches/baseline_suite.rs
  - crates/anofox-bench-harness/tests/dhat_peak.rs
  - scripts/update_criterion.sh
  - scripts/update_dhat.sh
  - .planning/baselines/criterion.json
  - .planning/baselines/dhat.json
autonomous: true
requirements: [PERF-01, PERF-03, PERF-04]
user_setup: []

estimate:
  tokens: 88000
  raw_tokens: 44000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`cargo bench --bench baseline_suite` runs fit+predict for all 7 representative families (AutoARIMA, AutoETS, AutoTheta, Naive, Croston, AutoEnsemble, Laplace) across single-series and batch-100 (PERF-01, D-07)"
    - "`.planning/baselines/criterion.json` carries the D-02 provenance fingerprint and a benchmarks array whose entries include both `profile: parallel` and `profile: no_parallel` sections (PERF-03, D-09)"
    - "`scripts/update_criterion.sh` captures median + MAD (not mean) per bench and OVERWRITES criterion.json idempotently; documented as local-only, never CI (D-03, MEAS-02)"
    - "`cargo test -p anofox-bench-harness --test dhat_peak` asserts peak heap bytes <= baseline x 1.15 for the major families using dhat::Alloc as the sole global allocator in that test binary (PERF-04, D-12)"
    - "`scripts/update_dhat.sh` captures HeapStats max_bytes per family into a provenance-stamped, idempotent dhat.json; on first run (no baseline) the dhat test captures-and-writes rather than failing on a missing baseline (PERF-04, MEAS-04 empty/first-run)"
    - "The dhat assertion uses <= baseline x 1.15 (peak strictly under or equal to 1.15x; explicit rounding via integer/float cast documented) (PERF-04 boundary)"
  artifacts:
    - benches/baseline_suite.rs
    - crates/anofox-bench-harness/tests/dhat_peak.rs
    - scripts/update_criterion.sh
    - scripts/update_dhat.sh
    - .planning/baselines/criterion.json
    - .planning/baselines/dhat.json
  key_links:
    - "baseline_suite.rs inlines the LCG fixture (does NOT dev-depend on anofox-bench-harness) to avoid the circular dependency (root crate ← harness crate ← root crate) — RESEARCH Open Question 3"
    - "dhat_peak.rs is the ONLY test file in the harness crate declaring #[global_allocator] = dhat::Alloc (one global allocator per test binary)"
    - "update_criterion.sh runs two profiles — `--features parallel` and default (no rayon) — writing distinct profile-tagged entries per D-09"
  prohibitions:
    - "update_criterion.sh / criterion baselines must NEVER be captured in CI (wall-clock noise — D-03 locked); criterion is informational only, never a CI gate"
    - "dhat_peak.rs must NOT declare more than one #[global_allocator]; no other harness integration test may declare a global allocator"
    - "Must NOT use wee_alloc as the allocator (banned); use dhat::Alloc for the profiling test only"
    - "Measurement code must NOT be added to library src/"
---

<objective>
Replicate the proven baseline pattern for the final two dimensions — criterion wall-clock (local, informational) and dhat peak-memory (native gate) — completing the four-dimension measurement backbone.

This plan: (1) adds `benches/baseline_suite.rs` — the single tracked criterion suite over the 7 representative families × {single, batch-100} in separate parallel / no_parallel profiles (PERF-01, PERF-03, D-06/D-07/D-09); (2) writes `scripts/update_criterion.sh` capturing median+MAD into `criterion.json` (D-03), local-only; (3) adds `crates/anofox-bench-harness/tests/dhat_peak.rs` asserting peak heap ≤ baseline × 1.15 for the major families (PERF-04, D-12) plus `scripts/update_dhat.sh`.

Purpose: complete the baseline store (all four JSON files committed) so Phases 2–4 have trustworthy before/after numbers for every compute/memory dimension.
Output: committed criterion.json + dhat.json; a runnable tracked criterion suite; passing dhat peak-memory tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-CONTEXT.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 03 slice)

**New bench:** `benches/baseline_suite.rs` (root crate) — criterion group `baselines` with `Criterion::default().sample_size(20)`; bench fns for AutoARIMA, AutoETS, AutoTheta, Naive, Croston, AutoEnsemble, Laplace, each covering single-series fit+predict and a batch-100 variant, run once per profile (parallel / no_parallel). Registered via `[[bench]] name = "baseline_suite", harness = false` in root `Cargo.toml`.
**New test:** `crates/anofox-bench-harness/tests/dhat_peak.rs` — `#[global_allocator] static ALLOC: dhat::Alloc`; per-family `#[test]` fns asserting `HeapStats::get().max_bytes <= (baseline * 115 / 100)`.
**New scripts:** `scripts/update_criterion.sh`, `scripts/update_dhat.sh`.
**New baseline files:** `.planning/baselines/criterion.json` (provenance + Vec<CriterionEntry{name,profile,median_ns,mad_ns}>), `.planning/baselines/dhat.json` (provenance + Vec<DhatEntry{name,peak_bytes}>).
**Consumes (from Plan 01):** `anofox_bench_harness::baseline::{ProvenanceFingerprint, CriterionEntry, DhatEntry}`, `anofox_bench_harness::fixtures::make_seeded_series` (dhat test only; baseline_suite inlines its own copy to avoid the dependency cycle).

<tasks>

<task type="auto">
  <name>Task 1: benches/baseline_suite.rs — 7 families x {single, batch-100} x {parallel, no_parallel}</name>
  <files>Cargo.toml, benches/baseline_suite.rs</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/benches/ets_benchmark.rs (criterion group/bench_function convention, LCG generator, TimeSeries construction — full file)
    - /home/simonm/projects/rust/anofox-forecast/benches/ensemble_benchmark.rs (AutoEnsemble construction pattern)
    - /home/simonm/projects/rust/anofox-forecast/src/batch.rs (batch::auto_ets signature for batch-100 variant, lines 51-70)
    - /home/simonm/projects/rust/anofox-forecast/Cargo.toml (existing [[bench]] registration block lines 74-108; `parallel = ["rayon"]` feature)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (baseline_suite.rs section + circular-dependency note)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Open Question 3 circular dep; Pitfall 6 parallel vs no-parallel)
  </read_first>
  <action>
    Add `[[bench]] name = "baseline_suite" \n harness = false` to root `Cargo.toml` (Edit, scoped — append to the existing [[bench]] block; do not rewrite the file).
    Write `benches/baseline_suite.rs`. RESOLVE the circular-dependency decision (RESEARCH Open Question 3): INLINE the ~10-line LCG `make_seeded_series` fixture directly in this file — do NOT add `anofox-bench-harness` as a dev-dependency of the root crate (that would create root ← harness ← root). State this choice in a top-of-file comment.
    Verify each family's constructor before use (read the model modules if the constructor name is uncertain): AutoARIMA, AutoETS (`::with_period(12)`), AutoTheta, Naive, Croston, AutoEnsemble, LaplaceForecaster — construct each via its documented `new`/`auto`/`with_*` constructor and drive it through the `Forecaster` fit + predict path. For each family provide a single-series bench (n=200 fit+predict) and a batch-100 bench. For the batch-100 AutoETS variant use the verified `batch::auto_ets(&values, 12, Some(12), None)` where `values: Vec<Vec<f64>>`; for families without a batch:: helper, loop fit+predict over 100 seeded series inside `b.iter`.
    PERF-03 / D-09 separate profiles: name benches so the profile is encoded (e.g. suffix `_parallel` vs `_no_parallel`), OR rely on the capture script running the whole suite twice (once `--features parallel`, once default) and tagging entries — pick the script-driven approach and keep bench IDs profile-agnostic, with the profile added by update_criterion.sh. Document which approach in a comment so update_criterion.sh (Task 2) matches.
    `criterion_group!` named form with `config = Criterion::default().sample_size(20)` (standardized per D-06), `criterion_main!(baselines)`. Run `cargo fmt`.
  </action>
  <verify>
    <automated>cargo build --bench baseline_suite 2>&1 | tail -5 && test $(cargo build --bench baseline_suite 2>&1 | grep -cE '^error') -eq 0 && test $(cargo build --bench baseline_suite --features parallel 2>&1 | grep -cE '^error') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build --bench baseline_suite` and `cargo build --bench baseline_suite --features parallel` both exit 0
    - The suite defines bench fns covering all 7 families: AutoARIMA, AutoETS, AutoTheta, Naive, Croston, AutoEnsemble, Laplace (grep the file for each name)
    - Each family has both a single-series and a batch-100 bench path
    - `baseline_suite.rs` does NOT import `anofox_bench_harness` (no dependency cycle) — `grep -c 'anofox_bench_harness' benches/baseline_suite.rs` returns 0
    - `Criterion::default().sample_size(20)` present (standardized sample size, D-06)
  </acceptance_criteria>
  <done>The tracked criterion suite compiles in both parallel and no-parallel configurations and covers all 7 representative families single + batch, with an inlined fixture and standardized sample size.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/update_criterion.sh — median+MAD, dual-profile, local-only</name>
  <files>scripts/update_criterion.sh, .planning/baselines/criterion.json</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Pattern 3 criterion capture + estimates.json structure; Assumption A2 verify field names; Pitfall 4 no-CI-capture)
    - /home/simonm/projects/rust/anofox-forecast/scripts/update_wasm_size.sh (Plan 01 provenance + idempotent python3 json.dump pattern)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-bench-harness/src/baseline.rs (CriterionEntry field names: name, profile, median_ns, mad_ns)
  </read_first>
  <action>
    Write `scripts/update_criterion.sh` (bash, `set -euo pipefail`, `chmod +x`). Top-of-file doc: "Run on a QUIET LOCAL MACHINE ONLY — never CI (wall-clock noise, D-03). Criterion baselines are informational, never a CI gate." Steps: (1) run `cargo bench --bench baseline_suite --features parallel` then `cargo bench --bench baseline_suite` (default = no rayon = WASM-comparable profile) — D-09 dual profiles. (2) For each profile, parse `target/criterion/<bench_id>/new/estimates.json`, extracting `median.point_estimate` (→ median_ns) and `median_abs_dev.point_estimate` (→ mad_ns) — VERIFY these field names against a real estimates.json produced by the run (Assumption A2). (3) Build `.planning/baselines/criterion.json` = `{ provenance: {...D-02...}, benchmarks: [ {name, profile, median_ns, mad_ns}, ... ] }` with one entry per (bench_id × profile); python3 `json.dump(indent=2)`, OPEN 'w' (idempotent overwrite; first-run creates). Use a small python3 helper (inline heredoc or `scripts/_build_criterion_json.py`) to walk the criterion dir. Commit `--no-verify`.
  </action>
  <verify>
    <automated>test -x scripts/update_criterion.sh && grep -qi 'local' scripts/update_criterion.sh && grep -q 'features parallel' scripts/update_criterion.sh && test $(grep -c 'baseline_suite' scripts/update_criterion.sh) -ge 2</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/update_criterion.sh` is executable, runs baseline_suite twice (once `--features parallel`, once default), and documents local-only capture
    - After running locally, `.planning/baselines/criterion.json` is valid JSON with provenance + a benchmarks array where `set(e['profile'] for e in benchmarks)` == {'parallel','no_parallel'} (PERF-03) and each entry has `median_ns` and `mad_ns` (D-03)
    - A second run overwrites (no duplicate/append growth)
    - The script contains no reference to any GitHub Actions / CI invocation
  </acceptance_criteria>
  <done>`scripts/update_criterion.sh` captures dual-profile median+MAD criterion baselines locally into a provenance-stamped, idempotent criterion.json.</done>
</task>

<task type="auto">
  <name>Task 3: dhat_peak.rs peak-memory tests (<= baseline x 1.15) + update_dhat.sh</name>
  <files>crates/anofox-bench-harness/Cargo.toml, crates/anofox-bench-harness/tests/dhat_peak.rs, scripts/update_dhat.sh, .planning/baselines/dhat.json</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Pattern 2 dhat test; Pitfall 3 single global allocator; D-12 x1.15)
    - /home/simonm/projects/rust/anofox-forecast/tests/batch_validation.rs (integration test structure analog)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-bench-harness/src/fixtures.rs (make_seeded_series — dhat test may use it since harness crate is not the root crate)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-bench-harness/src/baseline.rs (DhatEntry: name, peak_bytes)
    - /home/simonm/projects/rust/anofox-forecast/scripts/update_wasm_size.sh (provenance + idempotent write pattern)
  </read_first>
  <reversibility rating="reversible">D-12 headroom factor (1.15) is a test constant, adjustable later.</reversibility>
  <action>
    Write `crates/anofox-bench-harness/tests/dhat_peak.rs`. Declare `#[global_allocator] static ALLOC: dhat::Alloc = dhat::Alloc;` at file root — this MUST be the only global allocator in the harness crate's test binaries (Pitfall 3); do not add other integration tests to this file. Add one `#[test]` per major family (align with the PERF-01 representative set — at minimum AutoETS, AutoARIMA, Laplace; extend to the others if cheap): each builds `#[global_allocator]`-profiled work — `let _p = dhat::Profiler::builder().testing().build();`, run fit+predict on `make_seeded_series(1000, 42)`, then `let stats = dhat::HeapStats::get();`. Load the per-family baseline from `.planning/baselines/dhat.json` (a small `load_dhat_baseline(name) -> u64` helper reading the committed JSON). BOUNDARY (D-12): assert `stats.max_bytes <= (baseline_bytes as f64 * 1.15) as usize` — peak strictly under-or-equal to 1.15×; document the float→usize truncation. FIRST-RUN handling: if dhat.json is missing or lacks the entry, the test should SKIP the assertion (print a notice) rather than panic — the baseline is written by update_dhat.sh, not asserted into existence.
    Write `scripts/update_dhat.sh`: run the dhat tests in a capture mode (e.g. a `CAPTURE_DHAT=1` env the test reads to print `name peak_bytes` instead of asserting, OR a dedicated `--ignored` capture test), collect `max_bytes` per family, and write provenance-stamped `.planning/baselines/dhat.json` = `{provenance, benchmarks:[{name, peak_bytes}]}` via python3 `json.dump`, overwrite (idempotent; first-run creates). `set -euo pipefail`, `chmod +x`. Register nothing new in Cargo.toml unless a `[[test]]` entry is needed (integration tests auto-discover — Cargo.toml edit likely unnecessary; only touch it if required). Run `cargo fmt`; commit `--no-verify` for the JSON/script.
  </action>
  <verify>
    <automated>test -x scripts/update_dhat.sh && grep -q 'global_allocator' crates/anofox-bench-harness/tests/dhat_peak.rs && test $(grep -c 'global_allocator' crates/anofox-bench-harness/tests/dhat_peak.rs) -eq 1 && grep -q '1.15' crates/anofox-bench-harness/tests/dhat_peak.rs && cargo test -p anofox-bench-harness --test dhat_peak --no-run 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - `crates/anofox-bench-harness/tests/dhat_peak.rs` declares exactly one `#[global_allocator]` (dhat::Alloc)
    - `cargo test -p anofox-bench-harness --test dhat_peak --no-run` compiles (0 errors)
    - The assertion uses `<= baseline * 1.15` (grep for `1.15`) and skips gracefully when the baseline entry is absent
    - `bash scripts/update_dhat.sh` writes valid dhat.json with provenance + a benchmarks array of `{name, peak_bytes}` (integer) for the major families; second run overwrites without duplicating
    - After baseline capture, `cargo test -p anofox-bench-harness --test dhat_peak` passes
  </acceptance_criteria>
  <done>Peak-memory tests assert ≤ baseline × 1.15 for major families with a single dhat global allocator; dhat.json is captured with provenance and idempotent overwrite; first-run is graceful.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local capture script → baseline store | update_criterion.sh / update_dhat.sh write `.planning/baselines/*.json` on a developer machine (deliberate human action, never CI) |
| Test binary → global allocator | dhat_peak.rs swaps the process allocator; a second global allocator would break the binary |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-09 | Tampering | criterion baseline captured on noisy env | medium | mitigate | Script documents + is used local-only; criterion is informational, never a CI gate (D-03) — no CI step invokes it |
| T-01-10 | Denial of Service | dhat double global allocator | low | mitigate | Exactly one `#[global_allocator]` in dhat_peak.rs; no other harness integration test declares one (grep-verified) |
| T-01-11 | Tampering | dev-dependency dhat/criterion installs | low | accept | dhat 0.3.3 / criterion 0.5 verified OK in RESEARCH Package Legitimacy Audit; no [ASSUMED]/[SUS] packages |
| T-01-SC | Tampering | banned allocator substitution | high | mitigate | wee_alloc banned (archived, memory leaks); only dhat::Alloc used, only in the profiling test binary |
</threat_model>

<verification>
- `cargo build --bench baseline_suite` (both profiles) exits 0; all 7 families present
- `cargo test -p anofox-bench-harness --test dhat_peak --no-run` compiles; single global allocator
- `bash scripts/update_criterion.sh` (local) produces criterion.json with parallel + no_parallel profiles and median+MAD
- `bash scripts/update_dhat.sh` produces dhat.json with per-family peak_bytes + provenance
- No new files under library src/ (MEAS-04); no CI step captures criterion (D-03)
</verification>

<success_criteria>
The criterion and dhat dimensions are complete: the tracked suite covers all 7 families in separate parallel/no_parallel profiles with local median+MAD capture, and the dhat native test enforces the ≤1.15× peak-memory bound. With Plans 01–02, all four baseline JSON files are committed and the measurement backbone is fully stood up.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-infrastructure-compute-baselines/01-03-SUMMARY.md` when done.
</output>
