---
phase: 01-measurement-infrastructure-compute-baselines
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - crates/anofox-bench-harness/Cargo.toml
  - crates/anofox-bench-harness/src/lib.rs
  - crates/anofox-bench-harness/src/baseline.rs
  - crates/anofox-bench-harness/src/fixtures.rs
  - crates/anofox-forecast-js/src/forecaster.rs
  - crates/anofox-forecast-js/src/laplace_playground.rs
  - scripts/update_wasm_size.sh
  - .github/workflows/wasm-size.yml
  - .planning/baselines/wasm_size.json
autonomous: false
requirements: [MEAS-01, MEAS-02, MEAS-03, MEAS-04, PERF-05, PERF-06]
user_setup: []

estimate:
  tokens: 78000
  raw_tokens: 39000
  tasks: 4
  confidence: low

must_haves:
  truths:
    - "The new workspace member `anofox-bench-harness` builds: `cargo build -p anofox-bench-harness` exits 0 (D-04)"
    - "`.planning/baselines/wasm_size.json` exists, is valid JSON, and contains keys git_sha, timestamp_iso, rustc_version, host_cpu, host_os, active_features, filename, bytes (D-01, D-02)"
    - "`cargo build -p anofox-forecast-js --target wasm32-unknown-unknown 2>&1 | grep -c 'warning:'` returns 0 after cleanup (PERF-06)"
    - "`wasm-pack build crates/anofox-forecast-js --target web --out-dir ../../js --out-name anofox_forecast_js --release` exits 0 and the npm package (js/) still builds (PERF-06 backward-compat)"
    - "Running `bash scripts/update_wasm_size.sh` twice produces byte-identical wasm_size.json except the git_sha/timestamp_iso provenance fields — no append/duplicate growth (MEAS-01 idempotency)"
    - "`update_wasm_size.sh` refuses to write the baseline when dead-code warnings remain (PERF-06 sequencing guard) and handles a missing baseline file on first run by writing it, not crashing (MEAS-04 empty/first-run)"
    - "`wasm-size.yml` reads the committed baseline and fails only when the built .wasm grows strictly greater than baseline x 1.01; it contains no step that writes or commits into .planning/baselines/ (MEAS-01 CI-read-only, PERF-05 boundary)"
  artifacts:
    - crates/anofox-bench-harness/Cargo.toml
    - crates/anofox-bench-harness/src/lib.rs
    - crates/anofox-bench-harness/src/baseline.rs
    - crates/anofox-bench-harness/src/fixtures.rs
    - scripts/update_wasm_size.sh
    - .github/workflows/wasm-size.yml
    - .planning/baselines/wasm_size.json
  key_links:
    - "Root Cargo.toml [workspace] members includes crates/anofox-bench-harness (harness crate is a real workspace member, not orphaned)"
    - "update_wasm_size.sh writes the exact D-02 ProvenanceFingerprint field set that wasm-size.yml and all later dimensions read"
    - "PERF-06 cleanup lands and is confirmed zero-warning BEFORE update_wasm_size.sh captures the size baseline"
  prohibitions:
    - "The WASM-size baseline must NOT be captured or locked before PERF-06 dead-code removal is confirmed zero-warning"
    - "wasm-size.yml (and any CI workflow) must NOT write, git add, or commit anything under .planning/baselines/"
    - "Measurement code must NOT be added to library src/ (anofox-forecast/src/); harness code lives only in the new crate, benches/, tests/, scripts/"
    - "Must NOT introduce wee_alloc anywhere"
---

<!-- planner-discipline-allow: warning: -->

<objective>
Stand up the measurement backbone end-to-end through the WASM-size dimension — the tracer slice that proves the whole pattern the other three dimensions replicate.

This plan: (1) creates the `anofox-bench-harness` workspace crate that owns the D-02 provenance baseline schema and shared seeded fixtures (D-04); (2) removes the 10 confirmed WASM dead-code warnings in `crates/anofox-forecast-js/` (PERF-06); (3) writes the per-dimension maintainer entrypoint `scripts/update_wasm_size.sh` (D-05 — the `update_*.sh` pattern replicated by iai/criterion/dhat in Waves 2–3) which captures the release `.wasm` size into `.planning/baselines/wasm_size.json` with a full provenance fingerprint (MEAS-01, MEAS-02); (4) adds the `wasm-size.yml` CI gate that reads the baseline and fails on >1% relative growth (MEAS-03, PERF-05).

PERF-06 is sequenced BEFORE the WASM-size baseline capture: the baseline must reflect the post-cleanup binary, never the inflated pre-cleanup one.

Purpose: prove the baseline-store schema + local capture script + read-only CI gate on one dimension before replicating across iai/criterion/dhat.
Output: a working, committed WASM-size baseline; a building harness crate; zero WASM dead-code warnings.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-CONTEXT.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md
</context>

## Artifacts this phase produces (Plan 01 slice)

**New workspace crate:** `anofox-bench-harness` (lib name `anofox_bench_harness`, `publish = false`), path `crates/anofox-bench-harness/`.
  - `src/baseline.rs` — public serde structs (D-02): `ProvenanceFingerprint { git_sha, timestamp_iso, rustc_version, host_cpu, host_os, active_features }`, `CriterionEntry { name, profile, median_ns, mad_ns }`, `IaiEntry { name, instruction_count }`, `DhatEntry { name, peak_bytes }`, `WasmSizeBaseline { provenance, filename, bytes }` (plus per-dimension wrapper structs as needed).
  - `src/fixtures.rs` — public fn `make_seeded_series(n: usize, seed: u64) -> anofox_forecast::core::TimeSeries`.
  - `src/lib.rs` — `pub mod baseline; pub mod fixtures;`
**New script:** `scripts/update_wasm_size.sh`.
**New workflow:** `.github/workflows/wasm-size.yml`.
**New baseline file:** `.planning/baselines/wasm_size.json`.
**Modified:** root `Cargo.toml` (add harness crate to `[workspace] members`); `crates/anofox-forecast-js/src/forecaster.rs` and `.../laplace_playground.rs` (PERF-06 removals).
**PERF-06 symbols REMOVED (exact):** 8 unused `pub(crate) fn inner()` methods in `forecaster.rs` — `impl SESForecaster` (~L383-387), `impl HoltForecaster` (~L433-437), `impl HoltWintersForecaster` (~L515-520), `impl CrostonForecaster` (~L1207-1212), `impl TSBForecaster` (~L1259-1264), `impl ADIDAForecaster` (~L1313-1318), `impl IMAPAForecaster` (~L1367-1372), `impl GARCHForecaster` (~L1631-1636); the unused `RecipeKind` import in `laplace_playground.rs` (~L15); and the unnecessary `mut` on the KalmanFilter binding in `forecaster.rs` (~L2141). Do NOT remove `inner()` in `time_series.rs:144`, `calendar.rs:212`, `postprocess.rs:67` — those are USED.

<tasks>

<task type="tracer">
  <name>Task 1: Harness crate skeleton + D-02 baseline schema + seeded fixtures</name>
  <files>Cargo.toml, crates/anofox-bench-harness/Cargo.toml, crates/anofox-bench-harness/src/lib.rs, crates/anofox-bench-harness/src/baseline.rs, crates/anofox-bench-harness/src/fixtures.rs</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/Cargo.toml (root workspace members line 1-3; dev-deps criterion 0.5)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-forecast-js/Cargo.toml (path-dep pattern for anofox-forecast; serde derive dep)
    - /home/simonm/projects/rust/anofox-forecast/benches/ets_benchmark.rs (make_timestamps + generate_seasonal_series LCG analog, lines 10-30)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (Cargo.toml + fixtures.rs + baseline.rs sections)
  </read_first>
  <reversibility rating="costly">D-04 (new workspace member) touches Cargo.toml/lockfile and becomes the import root for all tooling; D-02 (baseline schema) is read by every compare path across Phases 1-4. Both flagged costly in CONTEXT.md — checkpoint:decision precedes this task.</reversibility>
  <action>
    Add `crates/anofox-bench-harness` to root `Cargo.toml` `[workspace] members` (append after `crates/anofox-forecast-js`; use Edit, scoped — do NOT rewrite the file). Create the harness crate: package name `anofox-bench-harness`, version 0.1.0, edition 2021, `publish = false`, lib name `anofox_bench_harness`. Dependencies: `anofox-forecast = { path = "../..", default-features = false, features = [] }`, `serde = { version = "1.0", features = ["derive"] }`, `serde_json = "1.0"`, `chrono = "0.4"`. Dev-dependencies (needed by later plans, declare now): `iai-callgrind = "0.16.1"`, `dhat = "0.3.3"`, `criterion = "0.5"`. Do NOT register any `[[bench]]` yet — later plans add those alongside their bench files so the crate keeps compiling.
    In `src/baseline.rs` define the D-02 serde structs listed in "Artifacts this phase produces": `ProvenanceFingerprint`, `CriterionEntry`, `IaiEntry`, `DhatEntry`, `WasmSizeBaseline`. Derive order `Serialize, Deserialize, Debug, Clone`; PascalCase types, snake_case fields per project conventions. `active_features: Vec<String>`; byte/count fields `u64`; timing fields `f64`.
    In `src/fixtures.rs` define `pub fn make_seeded_series(n: usize, seed: u64) -> TimeSeries` using the exact LCG from `ets_benchmark.rs` (multiplier 6364136223846793005, `>> 33`, `+20.0` offset), seed-parameterized per D-08. In `src/lib.rs`: `pub mod baseline;` and `pub mod fixtures;`.
    Confirm no measurement code entered library `src/` (MEAS-04) — everything is under `crates/anofox-bench-harness/`.
  </action>
  <verify>
    <automated>cargo build -p anofox-bench-harness 2>&1 | tail -5; test $(cargo build -p anofox-bench-harness 2>&1 | grep -cE '^error') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-bench-harness` exits 0
    - `cargo metadata --no-deps 2>/dev/null | grep -q anofox-bench-harness` (crate is a recognized workspace member)
    - `src/baseline.rs` defines `ProvenanceFingerprint` with all six fields git_sha, timestamp_iso, rustc_version, host_cpu, host_os, active_features
    - `git diff --name-only -- src/ | grep -c .` equals 0 (no new files under library src/ — MEAS-04)
  </acceptance_criteria>
  <done>Harness crate is a building workspace member; D-02 schema structs and `make_seeded_series` compile; no library src/ files touched.</done>
</task>

<task type="auto">
  <name>Task 2: PERF-06 — remove confirmed WASM dead code, confirm zero warnings + npm build</name>
  <files>crates/anofox-forecast-js/src/forecaster.rs, crates/anofox-forecast-js/src/laplace_playground.rs</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-forecast-js/src/forecaster.rs (the 8 inner() impl blocks + the mut binding around L2141 — read each region before deleting)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-forecast-js/src/laplace_playground.rs (the use statement around L15 with RecipeKind)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Dead Code Inventory — exact file:line list, lines 526-556)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (PERF-06 section — impl block deletion pattern)
  </read_first>
  <action>
    Delete the 8 unused `pub(crate) fn inner()` methods enumerated in RESEARCH Dead Code Inventory — remove the entire `impl { ... }` block for each of SESForecaster, HoltForecaster, HoltWintersForecaster, CrostonForecaster, TSBForecaster, ADIDAForecaster, IMAPAForecaster, GARCHForecaster (each impl contains only the `inner` method, so remove the whole block). Grep to locate exact current line numbers before editing (line numbers drift as you delete top-down — delete bottom-up or re-grep between edits). Remove `RecipeKind` from the `use anofox_forecast::models::laplace::{...}` statement in `laplace_playground.rs`. Remove the unnecessary `mut` from the KalmanFilter binding near `forecaster.rs:2141`. Do NOT touch the USED `inner()` methods in `time_series.rs`, `calendar.rs`, `postprocess.rs`. Keep the public JS-binding API surface unchanged — these are internal `pub(crate)` helpers, so removal is backward-compatible with `@sipemu/anofox-forecast`. Run `cargo fmt` before any commit (pre-commit hook requirement).
  </action>
  <verify>
    <automated>test $(cargo build -p anofox-forecast-js --target wasm32-unknown-unknown 2>&1 | grep -v '^#' | grep -c 'warning:') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-forecast-js --target wasm32-unknown-unknown 2>&1 | grep -v '^#' | grep -c 'warning:'` returns 0
    - `wasm-pack build crates/anofox-forecast-js --target web --out-dir ../../js --out-name anofox_forecast_js --release` exits 0
    - `cargo build -p anofox-forecast-js` (native) exits 0 — no compile breakage from removed methods
    - `grep -c 'RecipeKind' crates/anofox-forecast-js/src/laplace_playground.rs` returns 0
  </acceptance_criteria>
  <done>Zero dead-code warnings against anofox-forecast-js on the wasm target; release wasm-pack build succeeds; npm package still builds; public API unchanged.</done>
</task>

<task type="auto">
  <name>Task 3: scripts/update_wasm_size.sh — provenance-stamped, idempotent, PERF-06-guarded capture</name>
  <files>scripts/update_wasm_size.sh, .planning/baselines/wasm_size.json</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (scripts/update_*.sh section — full update_wasm_size.sh skeleton with warning guard + provenance python block)
    - /home/simonm/projects/rust/anofox-forecast/.github/workflows/npm.yml (wasm-pack build + git checkout js/package.json restore pattern)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-forecast-js/Cargo.toml (wasm-pack release profile / out artifact naming)
  </read_first>
  <action>
    Write `scripts/update_wasm_size.sh` (bash, `set -euo pipefail`, `chmod +x`). Step order: (1) GUARD — compute `cargo build -p anofox-forecast-js --target wasm32-unknown-unknown 2>&1 | grep -v '^#' | grep -c 'warning:'`; if non-zero, print an error naming PERF-06 and exit 1 (enforces PERF-06-before-PERF-05 sequencing). (2) `wasm-pack build crates/anofox-forecast-js --target web --out-dir ../../js --out-name anofox_forecast_js --release`, then `git checkout js/package.json js/README.md` to restore custom files. (3) Measure `stat --format=%s js/anofox_forecast_js_bg.wasm`. (4) Gather provenance: `git rev-parse HEAD`, ISO-8601 UTC timestamp, `rustc --version`, host CPU from /proc/cpuinfo (fallback `unknown`), `uname -sr`. (5) Write `.planning/baselines/wasm_size.json` as a single JSON object matching the `WasmSizeBaseline` schema (provenance fingerprint + filename `anofox_forecast_js_bg.wasm` + bytes) via a python3 heredoc, `json.dump(..., indent=2)` with a trailing newline. The write must OVERWRITE the file (open 'w'), never append — guaranteeing idempotency modulo git_sha/timestamp. If the file does not exist yet (first run), the same 'w' write creates it — no crash. Document at the top: "Run on a quiet local machine; captures the WASM size baseline AFTER PERF-06 cleanup." Since this is a non-Rust artifact, committing may need `git commit --no-verify` (cargo-fmt hook). Run the script to produce the committed `wasm_size.json`.
  </action>
  <verify>
    <automated>bash scripts/update_wasm_size.sh && python3 -c "import json; d=json.load(open('.planning/baselines/wasm_size.json')); assert all(k in d['provenance'] for k in ('git_sha','timestamp_iso','rustc_version','host_cpu','host_os','active_features')); assert d['filename']=='anofox_forecast_js_bg.wasm' and isinstance(d['bytes'],int) and d['bytes']>0; print('OK', d['bytes'])"</automated>
  </verify>
  <acceptance_criteria>
    - `bash scripts/update_wasm_size.sh` exits 0 and writes valid JSON with all D-02 provenance fields + filename + positive integer bytes
    - Running the script twice yields byte-identical files except git_sha/timestamp_iso: `d1=json; ...` — verify with `python3 -c` that keys `filename`, `bytes` are identical across two runs
    - The script exits 1 with a PERF-06 message if `cargo build -p anofox-forecast-js --target wasm32-unknown-unknown` emits any warning (verifiable by temporarily reintroducing a warning — document, do not leave reintroduced)
    - `test -x scripts/update_wasm_size.sh` (executable bit set)
  </acceptance_criteria>
  <done>`.planning/baselines/wasm_size.json` is committed with a full provenance fingerprint reflecting the post-PERF-06 binary; the script is idempotent and refuses to capture while warnings remain.</done>
</task>

<task type="auto">
  <name>Task 4: .github/workflows/wasm-size.yml — read-only >1% relative size gate</name>
  <files>.github/workflows/wasm-size.yml</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/.github/workflows/ci.yml (trigger block, checkout/toolchain/rust-cache steps, action pinning style)
    - /home/simonm/projects/rust/anofox-forecast/.github/workflows/npm.yml (wasm-pack build + git checkout restore)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (wasm-size.yml full structure)
    - /home/simonm/projects/rust/anofox-forecast/.planning/baselines/wasm_size.json (the baseline this workflow reads — must exist first, Task 3)
  </read_first>
  <reversibility rating="reversible">D-11 gate threshold (>1% relative) is a workflow value, trivially adjustable later.</reversibility>
  <action>
    Write `.github/workflows/wasm-size.yml`. Triggers: `push` and `pull_request` on `[main, master]`. Single job `wasm-size` on `runs-on: ubuntu-latest`, with `permissions: contents: read` (least privilege — this job only reads). Steps: checkout (`actions/checkout@v4`), `dtolnay/rust-toolchain@stable` with `targets: wasm32-unknown-unknown`, `Swatinem/rust-cache@v2`, install wasm-pack (curl init.sh — pin/verify per threat model), build release wasm (`wasm-pack build crates/anofox-forecast-js --target web --out-dir ../../js --out-name anofox_forecast_js --release`), `git checkout js/package.json js/README.md`, then a size-gate step: read `CURRENT=$(stat --format=%s js/anofox_forecast_js_bg.wasm)`, read `BASELINE` from `.planning/baselines/wasm_size.json` via python3, compute relative delta, and FAIL (exit non-zero) only when delta is strictly greater than 1.0% (`delta > 1.0`), pass at/under. The workflow MUST contain no step that writes, `git add`s, or commits under `.planning/baselines/` (CI reads, never writes — MEAS-01). Add an inline note that first-run-before-baseline is not a concern here because Task 3 already committed the baseline. Commit with `git commit --no-verify` (non-Rust artifact).
  </action>
  <verify>
    <automated>python3 -c "import yaml; w=yaml.safe_load(open('.github/workflows/wasm-size.yml')); assert 'wasm-size' in w['jobs']; assert w['jobs']['wasm-size'].get('permissions',{}).get('contents')=='read'; print('yaml OK')" && test $(grep -cE 'git add .*baselines|git commit.*baselines|>[[:space:]]*\.planning/baselines' .github/workflows/wasm-size.yml) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `wasm-size.yml` is valid YAML and defines a `wasm-size` job with `permissions: contents: read`
    - The workflow contains zero steps that write/`git add`/commit under `.planning/baselines/` (CI read-only invariant)
    - The gate step compares `stat`-measured current bytes to the committed baseline and fails only on `delta > 1.0` percent (strict greater-than)
    - Triggers are `push` and `pull_request` on `[main, master]`
  </acceptance_criteria>
  <done>`wasm-size.yml` reads the committed baseline and enforces the >1% relative growth gate, with least-privilege read-only permissions and no baseline mutation.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → third-party actions/installers | wasm-size.yml pulls actions and a curl-piped wasm-pack installer; untrusted upstream code executes in CI |
| CI job → repository baseline store | The gate reads `.planning/baselines/wasm_size.json`; a compromised job could try to rewrite it and mask a regression |
| PR author → CI workflow | A malicious PR could attempt to weaken the gate or exfiltrate the token via workflow edits |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-01 | Tampering | wasm-size.yml third-party actions | medium | mitigate | Pin `actions/checkout`, `dtolnay/rust-toolchain`, `Swatinem/rust-cache` to a known tag/SHA; document the wasm-pack installer source |
| T-01-02 | Tampering | baseline store write from CI | high | mitigate | `permissions: contents: read` on the job; no `git add`/commit/write step under `.planning/baselines/` — verified by grep gate in Task 4 |
| T-01-03 | Elevation of Privilege | GITHUB_TOKEN scope | medium | mitigate | Least-privilege `permissions: contents: read`; no secrets consumed by the job |
| T-01-04 | Information Disclosure | curl \| sh wasm-pack install | low | accept | Standard rustwasm installer over HTTPS; measurement-only CI, no secrets in scope; rationale documented inline |
| T-01-SC | Tampering | crate installs (harness dev-deps) | high | mitigate | iai-callgrind 0.16.1 / dhat 0.3.3 verified in RESEARCH Package Legitimacy Audit (all OK); no [ASSUMED]/[SUS]/[SLOP] packages — no human checkpoint required |
</threat_model>

<verification>
- `cargo build -p anofox-bench-harness` exits 0 (harness crate is a valid workspace member)
- `cargo build -p anofox-forecast-js --target wasm32-unknown-unknown 2>&1 | grep -v '^#' | grep -c 'warning:'` returns 0 (PERF-06)
- `wasm-pack build crates/anofox-forecast-js --target web --out-dir ../../js --out-name anofox_forecast_js --release` exits 0 (npm build intact)
- `.planning/baselines/wasm_size.json` is valid JSON with the full D-02 provenance fingerprint + filename + positive bytes
- `bash scripts/update_wasm_size.sh` is idempotent (byte-identical modulo git_sha/timestamp) and PERF-06-guarded
- `wasm-size.yml` is valid YAML, read-only, and gates on >1% relative growth
- `git diff --name-only -- src/` shows no new files under library src/ (MEAS-04)
</verification>

<success_criteria>
The WASM-size dimension works end-to-end: harness crate builds, dead code is gone and confirmed zero-warning, the baseline is captured post-cleanup with full provenance, and the CI gate reads (never writes) it and blocks >1% growth. This proves the baseline-store schema + local-capture-script + read-only-CI-gate pattern that Plans 02 and 03 replicate.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-infrastructure-compute-baselines/01-01-SUMMARY.md` when done.
</output>