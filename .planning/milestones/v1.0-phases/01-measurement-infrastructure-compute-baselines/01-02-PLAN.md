---
phase: 01-measurement-infrastructure-compute-baselines
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - crates/anofox-bench-harness/Cargo.toml
  - crates/anofox-bench-harness/benches/iai_suite.rs
  - scripts/update_iai.sh
  - .github/workflows/bench.yml
  - .planning/baselines/iai.json
autonomous: true
requirements: [PERF-02]
user_setup: []

estimate:
  tokens: 62000
  raw_tokens: 31000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "`cargo bench -p anofox-bench-harness --bench iai_suite` builds and runs the three hot paths (AutoETS fit, AutoARIMA fit, batch-100) under callgrind when valgrind is present (PERF-02)"
    - "`.planning/baselines/iai.json` exists, is valid JSON, carries the D-02 provenance fingerprint, and lists an IaiEntry {name, instruction_count} for each of the three hot paths (MEAS-01)"
    - "`scripts/update_iai.sh` captures instruction counts and OVERWRITES iai.json idempotently; on a first run with no prior baseline it writes the file rather than crashing (MEAS-01 idempotency + first-run)"
    - "`bench.yml` runs only the iai instruction gate (never `cargo bench --bench baseline_suite`), installs valgrind + iai-callgrind-runner 0.16.1 --locked, and fails CI when instruction count rises strictly greater than 1% over baseline (D-10, PERF-02 boundary)"
    - "`bench.yml` runs on pinned stable Rust only for the gate (beta/nightly do not gate instruction counts) and contains no step that writes/commits under .planning/baselines/ (D-10, MEAS-01 CI-read-only)"
  artifacts:
    - crates/anofox-bench-harness/benches/iai_suite.rs
    - scripts/update_iai.sh
    - .github/workflows/bench.yml
    - .planning/baselines/iai.json
  key_links:
    - "iai_suite.rs uses batch::auto_ets(&[Vec<f64>], period, Some(h), None) — the VERIFIED signature from src/batch.rs, not a &[TimeSeries] form"
    - "bench.yml installs iai-callgrind-runner at exactly 0.16.1 --locked (must match the iai-callgrind crate version, or the runner aborts)"
    - "iai.json entry names match the bench function/id names emitted by iai_suite.rs so update_iai.sh and the gate line up"
  prohibitions:
    - "bench.yml must NOT run `cargo bench --bench baseline_suite` or otherwise capture criterion wall-clock timings in CI (D-03 locked constraint)"
    - "The iai gate must NOT run on beta/nightly toolchains (instruction-count drift causes false positives — D-10)"
    - "No CI step may write/git add/commit under .planning/baselines/"
    - "Must NOT introduce wee_alloc"
---

<!-- planner-discipline-allow: baseline_suite -->

<objective>
Replicate the proven Plan-01 baseline pattern for the iai-callgrind instruction-count dimension — the hard compute gate for CI.

This plan: (1) adds `crates/anofox-bench-harness/benches/iai_suite.rs` measuring the three named hot paths (AutoETS fit, AutoARIMA fit, batch-100) with a 1% instruction-count soft limit (D-10, PERF-02); (2) writes `scripts/update_iai.sh` to capture instruction counts into `.planning/baselines/iai.json` with the D-02 provenance schema; (3) adds `.github/workflows/bench.yml` — the iai gate that installs valgrind + the version-locked runner, runs on pinned stable only, and fails on >1% instruction growth.

Purpose: make compute regressions blocking in CI on stable virtualized runners where callgrind counts are deterministic (criterion wall-clock stays local per D-03).
Output: a running iai bench, a committed iai baseline, and a green blocking bench.yml gate.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-CONTEXT.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md
@.planning/phases/01-measurement-infrastructure-compute-baselines/01-01-SUMMARY.md
</context>

## Artifacts this phase produces (Plan 02 slice)

**New bench:** `crates/anofox-bench-harness/benches/iai_suite.rs` — iai-callgrind `library_benchmark` fns `bench_auto_ets_fit`, `bench_auto_arima_fit`, `bench_batch_100`, grouped `hot_paths` with `Callgrind::default().soft_limits([(EventKind::Ir, 1.0f64)])`. Registered via a new `[[bench]] name = "iai_suite", harness = false` in `crates/anofox-bench-harness/Cargo.toml`.
**New script:** `scripts/update_iai.sh`.
**New workflow:** `.github/workflows/bench.yml`.
**New baseline file:** `.planning/baselines/iai.json` (schema: provenance + Vec<IaiEntry>).
**Consumes (from Plan 01):** `anofox_bench_harness::fixtures::make_seeded_series`, `anofox_bench_harness::baseline::{ProvenanceFingerprint, IaiEntry}`.

<tasks>

<task type="auto">
  <name>Task 1: Verify batch API + write iai_suite.rs (3 hot paths, 1% Ir soft limit)</name>
  <files>crates/anofox-bench-harness/Cargo.toml, crates/anofox-bench-harness/benches/iai_suite.rs</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/src/batch.rs (VERIFY the auto_ets signature — lines 51-70: `auto_ets(values: &[Vec<f64>], period: usize, horizon: Option<usize>, pool: Option<ModelPool>)`; it takes &[Vec<f64>], returns Vec<Result<(Forecast, ETSSpec)>>)
    - /home/simonm/projects/rust/anofox-forecast/benches/ets_benchmark.rs (AutoETS::with_period + Forecaster fit convention)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-bench-harness/src/fixtures.rs (make_seeded_series from Plan 01)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Pattern 1 iai bench + Open Question 5 batch signature; NOTE the RESEARCH skeleton's `batch::auto_ets(&series, 12)` call is WRONG — use the verified 4-arg &[Vec<f64>] form)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (iai_suite.rs section)
  </read_first>
  <action>
    First confirm the batch signature by reading `src/batch.rs` lines 51-70 — `batch::auto_ets` takes `&[Vec<f64>]` (raw value vectors), a `period: usize`, `horizon: Option<usize>`, and `pool: Option<ModelPool>`. The `bench_batch_100` setup must therefore build `Vec<Vec<f64>>` of 100 series (call `make_seeded_series` then extract its values, OR build raw Vec<f64> directly with the same LCG), NOT `Vec<TimeSeries>`; call `batch::auto_ets(&values, 12, Some(12), None)`.
    Add `[[bench]] name = "iai_suite" \n harness = false` to `crates/anofox-bench-harness/Cargo.toml`.
    Write `iai_suite.rs` using iai-callgrind 0.16.1 macros (`library_benchmark`, `library_benchmark_group`, `main`, `LibraryBenchmarkConfig`, `Callgrind`, `EventKind`) and `std::hint::black_box`. Three `#[library_benchmark]` fns: `bench_auto_ets_fit` (setup `make_seeded_series(200, 42)`, `AutoETS::with_period(12)`, fit), `bench_auto_arima_fit` (setup n=200, `AutoARIMA::new()`, fit), `bench_batch_100` (setup 100 raw value-vectors of length 100 via the LCG, `batch::auto_ets(&values, 12, Some(12), None)`). Group `hot_paths` with `config = LibraryBenchmarkConfig::default().callgrind(Callgrind::default().soft_limits([(EventKind::Ir, 1.0f64)]))` — the 1% Ir gate (D-10). `main!(library_benchmark_groups = hot_paths)`. Do NOT run baseline_suite here. Run `cargo fmt` before commit.
  </action>
  <verify>
    <automated>cargo build -p anofox-bench-harness --benches 2>&1 | tail -5 && test $(cargo build -p anofox-bench-harness --benches 2>&1 | grep -cE '^error') -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `cargo build -p anofox-bench-harness --benches` exits 0 (iai_suite.rs compiles against the real batch/AutoETS/AutoARIMA APIs)
    - `grep -c 'soft_limits' crates/anofox-bench-harness/benches/iai_suite.rs` >= 1 and references `EventKind::Ir` with `1.0`
    - `bench_batch_100` calls `batch::auto_ets` with a `&[Vec<f64>]`-typed argument and 4 args (verified against src/batch.rs)
    - `[[bench]] name = "iai_suite"` present in `crates/anofox-bench-harness/Cargo.toml`
  </acceptance_criteria>
  <done>iai_suite.rs compiles, registers as a harness=false bench, and encodes the 1% Ir soft limit over the three hot paths using the verified batch signature.</done>
</task>

<task type="auto">
  <name>Task 2: scripts/update_iai.sh — capture instruction counts to iai.json</name>
  <files>scripts/update_iai.sh, .planning/baselines/iai.json</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Open Question 2 — iai-callgrind first-run baseline behavior; the script must parse iai output / callgrind results rather than assume a built-in save)
    - /home/simonm/projects/rust/anofox-forecast/scripts/update_wasm_size.sh (Plan 01 — reuse the provenance-gathering + python3 json.dump idempotent-overwrite pattern)
    - /home/simonm/projects/rust/anofox-forecast/crates/anofox-bench-harness/src/baseline.rs (IaiEntry + ProvenanceFingerprint field names to match)
  </read_first>
  <action>
    Write `scripts/update_iai.sh` (bash, `set -euo pipefail`, `chmod +x`). Resolve RESEARCH Open Question 2 during implementation: run `cargo bench -p anofox-bench-harness --bench iai_suite` and determine how iai-callgrind 0.16.1 exposes instruction counts — prefer parsing the callgrind output files under `target/iai/` (or the runner's summary JSON if emitted) for the `Ir` event per benchmark id; if only stdout is available, parse the printed `Instructions` line per bench. Extract one instruction count per hot path (bench_auto_ets_fit, bench_auto_arima_fit, bench_batch_100). Gather the same D-02 provenance fields as update_wasm_size.sh (git_sha, ISO timestamp, rustc --version, host cpu, uname -sr, active_features []). Write `.planning/baselines/iai.json` as `{ provenance: {...}, benchmarks: [ {name, instruction_count}, ... ] }` via python3 `json.dump(indent=2)`, OPEN 'w' (overwrite → idempotent; first-run creates). Document "run on any machine with valgrind installed; instruction counts are deterministic". Commit with `git commit --no-verify`.
  </action>
  <verify>
    <automated>test -x scripts/update_iai.sh && grep -qE 'iai_suite' scripts/update_iai.sh && grep -q 'json' scripts/update_iai.sh && echo "script shape OK"</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/update_iai.sh` is executable and invokes `cargo bench -p anofox-bench-harness --bench iai_suite`
    - When valgrind is available, `bash scripts/update_iai.sh` writes `.planning/baselines/iai.json` as valid JSON with provenance + a `benchmarks` array of `{name, instruction_count}` (integer) entries for all three hot paths
    - The script overwrites (never appends) iai.json — a second run yields the same benchmark set with no duplicate entries
    - If valgrind is absent locally, the script fails with a clear message (does not silently write a bad baseline)
  </acceptance_criteria>
  <done>`scripts/update_iai.sh` captures per-hot-path instruction counts into a provenance-stamped, idempotent iai.json.</done>
</task>

<task type="auto">
  <name>Task 3: .github/workflows/bench.yml — stable-only iai instruction gate (>1% blocks)</name>
  <files>.github/workflows/bench.yml</files>
  <read_first>
    - /home/simonm/projects/rust/anofox-forecast/.github/workflows/ci.yml (trigger block, checkout/toolchain/rust-cache, taiki-e/install-action pattern, action pinning)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-PATTERNS.md (bench.yml full structure)
    - /home/simonm/projects/rust/anofox-forecast/.planning/phases/01-measurement-infrastructure-compute-baselines/01-RESEARCH.md (Pitfall 1 runner version match, Pitfall 2 valgrind version, D-10 stable-only)
    - /home/simonm/projects/rust/anofox-forecast/.planning/baselines/iai.json (baseline the gate reads — must exist first, Task 2)
  </read_first>
  <reversibility rating="reversible">D-10 gate threshold and toolchain pin are workflow values, adjustable later.</reversibility>
  <action>
    Write `.github/workflows/bench.yml`. Triggers: push + pull_request on `[main, master]`. `permissions: contents: read` (least privilege). Single job `iai-gate` on `runs-on: ubuntu-latest` (= 24.04, valgrind 3.22 ≥ required 3.20). Steps: checkout@v4 (pinned), `dtolnay/rust-toolchain@stable` (D-10 — stable ONLY, no beta/nightly matrix for the gate), `Swatinem/rust-cache@v2`, install valgrind (`sudo apt-get update && sudo apt-get install -y valgrind`), install runner (`cargo install iai-callgrind-runner --version 0.16.1 --locked` — must EXACTLY match the crate version per Pitfall 1), run gate (`cargo bench -p anofox-bench-harness --bench iai_suite`). The 1% Ir soft limit encoded in iai_suite.rs makes the bench itself exit non-zero on >1% growth vs its stored baseline; ensure the step surfaces that non-zero exit as a CI failure (do not swallow it). Do NOT add any `cargo bench --bench baseline_suite` step (D-03). No step writes/commits under `.planning/baselines/`. Pin third-party actions to tag/SHA per threat model. Commit `--no-verify`.
  </action>
  <verify>
    <automated>python3 -c "import yaml; w=yaml.safe_load(open('.github/workflows/bench.yml')); j=w['jobs']['iai-gate']; assert j.get('permissions',{}).get('contents')=='read'; print('yaml OK')" && test $(grep -c 'baseline_suite' .github/workflows/bench.yml) -eq 0 && grep -q '0.16.1 --locked' .github/workflows/bench.yml</automated>
  </verify>
  <acceptance_criteria>
    - `bench.yml` is valid YAML, job `iai-gate`, `permissions: contents: read`
    - `grep -c 'baseline_suite' .github/workflows/bench.yml` returns 0 (no criterion wall-clock capture in CI — D-03)
    - The workflow installs `iai-callgrind-runner --version 0.16.1 --locked` and valgrind
    - Uses `dtolnay/rust-toolchain@stable` with no beta/nightly toolchain in the gate job (D-10)
    - No step writes/`git add`s/commits under `.planning/baselines/`
  </acceptance_criteria>
  <done>`bench.yml` runs the stable-only iai instruction gate, blocking on >1% growth, without capturing criterion timings or mutating baselines.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CI runner → `cargo install` network fetch | bench.yml fetches iai-callgrind-runner from crates.io; unpinned versions could pull tampered/incompatible code |
| CI job → baseline store | The gate reads `.planning/baselines/iai.json`; a compromised job could rewrite it to mask a regression |
| PR author → bench.yml | A PR could weaken the gate (switch toolchain, drop --locked) |

## STRIDE Threat Register (ASVS L1; block-on: high)

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | iai-callgrind-runner install | high | mitigate | Pin `--version 0.16.1 --locked`; Cargo.lock respected; version verified in RESEARCH Package Legitimacy Audit |
| T-01-06 | Tampering | baseline store write from CI | high | mitigate | `permissions: contents: read`; no write/commit step under `.planning/baselines/` — grep-verified in Task 3 |
| T-01-07 | Elevation of Privilege | GITHUB_TOKEN scope | medium | mitigate | Least-privilege read-only permissions; job consumes no secrets |
| T-01-08 | Tampering | third-party actions (checkout/toolchain/cache) | medium | mitigate | Pin actions to tag/SHA |
</threat_model>

<verification>
- `cargo build -p anofox-bench-harness --benches` exits 0
- `bash scripts/update_iai.sh` (with valgrind) writes valid provenance-stamped iai.json with three IaiEntry rows
- `bench.yml` is valid YAML, stable-only, read-only, installs the version-locked runner, and never runs baseline_suite
- No new files under library src/ (MEAS-04)
</verification>

<success_criteria>
The iai instruction-count dimension is fully wired: the bench compiles and encodes the 1% gate, the baseline is captured with provenance, and bench.yml blocks compute regressions on stable CI without touching criterion timings or the baseline store.
</success_criteria>

<output>
Create `.planning/phases/01-measurement-infrastructure-compute-baselines/01-02-SUMMARY.md` when done.
</output>
